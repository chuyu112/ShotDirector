# 漫镜（Manjing）项目记忆

## 2026-09-05 严格审核调用修复

- 线上复现：Shot 01 的 JK Claude Opus 5 非流式请求约 61 秒返回上游 504；GLM 5.3 在旧 16384 输出预算下约 238 秒返回 length。
- 修复为审核专用预算、GLM/Claude 流式响应、Claude adaptive + output_config.effort=max。只接收具有完整结束标记的报告，失败记录仅保留安全的协议元数据和数值用量。
- 已确认的截断允许在同一 Run 内分两组检查补审，始终携带整个 Shot 和全部画格，不修改 Shot 分组、顺序或时长；阶段完成前不提交最终报告。网络结果不明确时仍要求人工重试，避免重复调用。
- 审核模型状态明确区分配置就绪、近期成功和近期失败。测试通过与线上真实模型成功必须分开汇报。
- 实测后续：Claude 流式请求开始返回后仍在约 101 秒被上游断开；不得宣称已解决上游稳定性。GLM MAX 约 12 分 32 秒返回完整报告（9 条意见），仅漏 reviewerId 导致原校验拒收；补齐服务端路由身份后的真实报告重放通过，审核正文没有更改或重新生成。此类遗漏可由服务端绑定身份，显式错误身份、缺失业务字段仍拒收。

## 2026-09-05 API 模型链路

- 文字模型改为 API-only：顶部是独立的 Chat / Work（Creator）模型，严格审核页是独立 Reviewer 模型，两者选择不互相覆盖。旧项目的 `writing-model-selection.json` 若保存 `codex-gpt-5.6-sol`／`gpt-5.6-sol`，载入时原地改写为 `jk-gpt-5.6-sol`；旧 Luna 同理改写为 `jk-gpt-5.6-luna`。公开目录与项目文件不再保留旧标识，也不再调用 Codex CLI。
- 已对齐翠易 10 个 Chat／Work 模型和 5 个 Reviewer 模型，新增 JK GPT Responses、JK Gemini Chat Completions 和 JK Claude Anthropic Messages 适配层。不做静默 fallback，API Key 只在 Gateway／Worker 环境中传递。

## 2026-09-03 阅读顺序约束

- 用户确认不重新裁剪，只修复阅读顺序和排版。第6话第2张右页的现有裁图 ID 顺序应为 R-G02 → R-G01 → R-G03 → R-G05 → R-G06 → R-G04；ID 不重命名、图片不互换，其中 R-G04 现有复合裁图保持原样。
- 日漫先右页后左页，页内按行列分区从上到下、同排从右到左。不是整页/整组倒序；缺少可靠分隔的叠格保留原序供人工复核。现有7个 Shot 的归属与批注保留，校正顺序不得被拖放操作隐式触发。

更新时间：2026-08-29

## 已核实状态

- 2026-08-30 漫镜正式目标为 `https://manjing.jadecircle.cn`，DNS 指向 `121.41.54.168`。
- 2026-08-29 目标服务器健康检查已核实 SQLite WAL schema 3、LibTV CLI `1.1.3`、FFmpeg `4.2.2-static`，Web/Gateway/Nginx 均 active。本次发布的文字链路使用 Kimi K3 的服务器 API Key；GLM-5.3-Flash 路由和 Schema 已就绪，但当前没有 GLM Key。顶部写作目录固定显示 GLM-5.3-Flash、Kimi K3、GPT-5.6 Luna、DeepSeek V4 Flash、Seed 2.1 Pro；只有配置并验收过的 GLM／Kimi 可选择，选择按项目写入 `work/writing-model-selection.json` 并影响真实 Worker 调用。GPT-5.6 按当前要求停用，DeepSeek／Seed 待接入；OpenAI 图片与 LibTV 付费生成额度仍默认为 0。
- 2026-08-29 视频分析已在目标 Linux 以 `manjing` 服务账号实测：3.02 秒测试视频产出 3 张逐秒帧、1 张时间码接触表及音频静音分析。`scripts/extract_every_second.py` 已兼容系统 Python 3.6 与 Linux 字体；FFmpeg 安装器校验 wheel 和二进制的两个 SHA-256。
- 2026-08-29 服务器上线加固已完成：SQLite/WAL/SHM `0600`，数据目录 `0700`；每用户/全服双层原子额度；会话、项目、Worker 数上限；空闲回收；可控 XFF 信任；短信节流；优雅 drain 与强制终止留痕。租户浏览器缓存与 IndexedDB 按 `userId + projectId` 分区，跨标签退出同步。
- 2026-08-29 最终回归：`npm test` 生产构建及 127/127 全量测试通过，`npm run test:server` 72/72，串行生产链 2/2，`npm run lint -- --quiet` 为 0 error。新增覆盖五模型目录、禁用项、项目级 GLM/Kimi 真切换、租户隔离，以及服务器误配 OpenAI 写作时的真实进程级 fail-closed。Next 16.3.3、React 19.2.8、Sharp 0.35.4、Vinext 1.0.0-beta.8 已升级；生产依赖审计无 high/critical，仅余 1 个与 Windows 开发服务器相关的 low。
- 2026-08-29 Kimi K3 已用小额真实 API 请求通过结构化文字和多模态看图合约验证；GLM 因无 Key 只完成本地合约测试，不得宣称真实调用通过。
- 2026-08-29 漫画 5–40 页按每批最多 4 页分别执行框检测、分析和重裁；40 页 Fake E2E 验证了 80 Shot 的全局页码、时间码与资产合并。历史框计划复用已绑定有序页码和内容 SHA-256，同名不同图或无哈希的旧记录不复用。

- 2026-08-28 新增漫镜服务器版：响应式 Web／手机页面共用同一账户与项目，支持邮箱注册、scrypt 密码、HttpOnly Session、项目新建／切换、`userId + projectId` 浏览器与磁盘隔离。公开网关为 `server/manjing-gateway.mjs`，认证库为 SQLite WAL，租户 Worker 只绑定回环端口。
- 2026-08-28 服务器模型链路当时曾以 OpenAI Responses API 为方案；该方案已被 2026-08-29 的 GLM/Kimi 文字路由取代。OpenAI Responses/Codex CLI 只保留为本地 legacy 兼容代码，服务器 Worker 与发布预检会硬拒绝其作为写作 provider；GPT 资产图仍是独立、默认额度为 0 的可选能力。每次模型调用在结果旁写 `.usage.json`。API Key 只存在服务器环境，租户 Worker 只继承环境白名单，浏览器不获取真实内部令牌。
- 2026-08-28 LibTV 服务器 Worker 支持 Linux、手机号／短信验证码／可选 captcha 无头登录、项目 FIFO 与付费失败不自动重提。Docker 镜像固定 LibTV 1.1.3，amd64 SHA-256 为 `cf86f462c5aed60f95dca978cc91ece98c60bcfa27337da008ad59953c3ea7da`，arm64 为 `369b43f5be1d28dbbde7c1b6711ed746bf9bff1028ba794a6dae4fa01bed601c`。
- 2026-08-28 服务器防滥用边界已加入持久化每日 AI／LibTV／短信／上传额度、项目上限、单用户 Worker 上限、全局 Worker 上限、空闲 LRU 回收和最长 15 分钟关机 drain。这些是可控用户运营的安全闸门，不是公众 SaaS 计费或恶意代码沙箱。

- 2026-08-28 产品可见名称从“镜导（ShotDirector）”改为“漫镜（Manjing）”。当前工作目录仍为 `/Users/Admin/Documents/Documents/镜导 ShotDirector`，旧目录名、浏览器 storage key、`work/shotdirector-*` 运行目录、桥接脚本文件名和旧请求头／环境变量暂不物理迁移，以保证现有漫画草稿、白模、生成结果和 LibTV 绑定可继续恢复。
- 2026-08-28 已把 `/Users/Admin/Documents/Documents/翠易内部导演台` 的成熟 Pi AgentSession Harness 核心移植为 `runner/manjing-pi-harness.mjs`，并使用 `@earendil-works/pi-coding-agent` 0.84.2。当前 Creator 支持 checkpoint 恢复，Reviewer 使用 per-Run 隔离 Session；Pi turn / message / tool / queue / compaction 事件写入脱敏账本。
- 2026-08-28 新增 `runner/manjing-harness-store.mjs`：本地保存 Session、Run、`events.jsonl`、stateVersion 乐观并发检查和每 Session 最多 50 份原子历史快照。桥接中的漫画分析、结构化生成、完整提示词和独立 Reviewer 已统一经该持久 Harness 执行。
- 2026-08-28 参考公开 MIT 项目 Kunpeng Director commit `1c629c9` 的稳定 ID、人工锁定、先读状态、完成后验证和原子历史设计，新增 `app/manjing-agent-contract.mjs` 并写入项目 manifest。没有替换原有白模实现，也没有迁入翠易账号、密钥、生产资产、员工权限或公司知识库。
- 旧 `X-ShotDirector-Token` 与 `SHOTDIRECTOR_*` 仍被桥接接受；新前端与文档使用 `X-Manjing-Token` 和 `MANJING_*`。LibTV 优先使用“漫镜 Manjing”，但会复用已有 bridge state 或旧“镜导 ShotDirector”工作区。

- 历史记录：2026-08-07 产品曾从“镜审（FrameLock）”改名为“镜导（ShotDirector）”；当时记录的根目录是 `/Users/Admin/Desktop/镜导 ShotDirector`，现已迁至 Documents 下并再次改名为“漫镜”。
- 当前目录已经接入 Git，分支为 `main`，远端为 `https://github.com/chuyu112/ShotDirector.git`。
- 2026-08-28 服务器版交付回归：`npm test` 生产构建成功并通过 81/81 项测试；`npm run test:server` 通过 32/32 项服务器专项测试；`npm run lint` 为 0 错误、19 条既有警告。测试未发起真实 OpenAI 请求、短信或 LibTV 付费生成。
- 构建仍有前端 chunk 大于 500 KB 的警告。
- `npm run lint` 通过，无 error，有 18 个 warning，主要集中在 `app/page.tsx` 的 Hook 依赖、未使用参数和原生 `<img>`。
- `app/manga-panel-mapping.mjs` 中旧项目专用修复已经删除；交接手册把它描述为仍含硬编码，属于过时信息。
- Codex CLI 运行参数已加入 `--skip-git-repo-check`，可在不含 `.git` 的源码迁移包中执行素材分析。
- `media-analysis.schema.json` 已移除 `$ref` 的同级 `type` 关键字，当前 GPT 结构化输出接口已实测接受该 schema。

## 当前漫画拆镜草稿

- 2026-08-15 用户要求在完整提示词生成后增加独立 Reviewer 环节。固定流程为“生成讨论稿 → 独立 Reviewer 审查 → 显示问题与修改建议 → 用户讨论／修改 → 用户明确批准”。Reviewer 无权改稿或批准；改稿后旧报告立即过期。首批 Reviewer 为 GPT-5.6-sol 独立 Agent、Kimi K3、GLM-5.2，架构必须允许继续注册其他模型。相同基础模型可以参与生成和审查，但必须是全新隔离任务与不同 requestId，不能让同一个生成任务自己审自己。

- 2026-08-15 用户确认 Shot 是 AI 生成的“稳定参考集合边界”：梦境→现实、外景→室内，或主要人物、服装、场景、光线、时间状态需要整体换参考时，必须优先拆成新 Shot。同一 Shot 尽量不换人物、服装和场景参考。第4话当前硬边界为：Shot 01 只含 `P01-L-G01`至`P02-R-G02`的砖廊噩梦，结束在BMW男子冷蓝面部特写；菜摘从`P02-R-G03`惊醒开始属于 Shot 02，因为此处同时切场景、切人物并更换多组参考。
- 2026-08-15 第4话草稿 `944a8e49-2dc6-4fd4-96a9-0389ccc77fe0` 的 Shot 01 已按新分组重新生成只含砖廊噩梦的完整提示词讨论稿，菜摘惊醒与现实卧室只保留在 Shot 02。Shot 01 的旧自动批准状态已撤销，必须先完成独立 Reviewer 审查，再由用户明确批准。重新编组后必须作废旧提示词与审查报告，并仅根据当前 `sourcePanels` 重建内容。

- 2026-08-07 已完成本话全部 12 张横向扫描图的分析，任务编号为 `a7501ea0-9880-47b3-a61a-8ebcadb2f46f`；结果为 141 个画格，其中 137 个进入剧情，默认组合为 20 个连续 Shot。137 个有效画格全部且仅分配一次，无遗漏、无重复，结构仍保持未确认状态。
- 完整结果可通过 `/?importDraft=<任务编号>` 从本地桥接恢复并自动创建独立草稿；不需要重新上传，也不会覆盖原审批草稿。
- 浏览器会记住最近打开的素材草稿；直接访问 `http://localhost:3000/` 时，优先载入浏览器中的最近草稿，如果浏览器临时状态缺失，则从本地桥接恢复最近完成的漫画分析。素材草稿中的“打开主工作区”使用 `/?main=1`，可明确绕过自动返回。
- 当前草稿会在浏览器本地保存的同时镜像到本机桥接目录 `work/shotdirector-draft-state/`；这样 Agent 可以直接读取用户刚完成的画格组合、删除、时长和批注，不再要求用户重复截图或口述。镜像只发送到 `127.0.0.1`，浏览器本地状态仍是权威来源。
- 2026-08-07 已把用户上传的 `3-1.png`、`3-2.png`、`3-3.png` 拆为《城市猎人》第3话“犯下大错！之卷”的独立审核草稿。
- 结果包含 4 个剧情页、26 个有效画格；合并为 6 个连续编号 Shot，当前时长为 8、8、8、8、12、8 秒，总长 52 秒；脚本体检显示 100% 覆盖、0 条未覆盖。原 11 镜结果已备份为 `work/shotdirector-responses/media-analysis-9e24edc2-0516-4094-ae04-eab3bd3cb3a5.pre-merge-11shots.json`。
- 用户在 2026-08-07 将时长规则更新为 6–15 秒；短促动作或单一反应允许 6 秒，Step 00 提供 6、8、10、12、15 秒快捷档。
- 2026-08-08 用户启用 Seedance 2.5：模型切换为时长上限的权威来源。Seedance 2.0 保持 6–15 秒；Seedance 2.5 为 6–30 秒，Step 00 额外提供 20、25、30 秒快捷档。多画格 Shot 不按图片数量平均分配时长，惊讶、回头、抓握和动作特写可作为 0.3–1 秒的瞬时节点。
- 2026-09-02 新建项目默认使用 Seedance 2.5；新建空白 Shot 默认 30 秒。加载旧项目时保留用户已经保存的模型与逐镜时长，不用新默认值覆盖。
- 当前草稿已切换为 Seedance 2.5；Shot 03 保留用户组合的 20 张画格，时长 30 秒，整理为 7 个连续时间段。
- Shot 03 的完整提示词已于 2026-08-08 生成并回填，sourceRevision 为 `complete-1c7p9di`，提示词 4842 字，结果文件为 `work/shotdirector-responses/complete-shot-prompt-f000dfd9-b61a-4fa1-9e0e-97617ce3bdf1.committed.json`。页面先前在任务刚启动时将“暂无可恢复结果”误判为失败；现已改为生成中只在收到同 Shot 的终态任务后才尝试恢复。联网查询已尝试但无可用来源时，允许 `research.used=false`且保留 queries 记录。
- 2026-08-08 以用户实测可用的 Shot 02（六句日语共约105个有效字符，15秒刚好）校准估时：日语成片对白约7个有效字符/秒，中文草稿对白约4个汉字/秒；两者都已经包含正常标点和换气，不再重复追加停顿。Shot总时长取“全部对白时间”和“完整视觉动作链时间”的较大值，因为对白、运镜、包扎、走动、倒下和听者反应可以并行；瞬时反应画格允许0.3–1秒，不按画格数量平均分秒。除总体估时外还要逐句检查局部时间窗，单句窗口不得短于该句有效字符数÷7。
- 第2话连续性已写入背景：1987 年冬夜新宿／歌舞伎町、冴羽獠左掌贯通伤、黑色 BMW 尚未落网、菜摘与槇村的状态，以及象牙白围巾不得无依据重现。
- 最终成片风格已改为昭和62年（1987年）东京新宿背景的写实日本真人都市动作电影；全部角色由真实日本演员出演，强调35mm胶片、真实皮肤与衣料、可信物理动作和昭和末期实景。最终美术风格只使用正面的真人电影语言描述演员、摄影、灯光、材质、年代和表演。
- 草稿已进入逐镜审核区，保持“独立保存”，不会覆盖原审批；当前 0/6 Shot 已批准，镜头结构尚待用户确认。
- 新流程增加 Step 00 画格编组台：当前 26 张有效画格全部以缩略图展示，并按系统建议分成 6 个 Shot。用户可整组选中、Shift 连选、把相邻画格重新组合为一个 Shot、并入前后组、拆成单格、删除画格和撤销；自动重编号与重排时间码。结构确认前不渲染资产提示词，不解锁分镜图与视频提示词；确认后才生成下游数据。
- 当前素材草稿顶部有“续传漫画”入口，进入后继续通过“选择一页或多页漫画”追加扫描图；已有 3 张仍保留在队列中。未来“新增手绘分镜”入口已占位但禁用。
- 画格编组台的每张缩略图带纯图标放大按钮；全屏查看器保持 100% 适屏，支持上一张/下一张、方向键和 Esc。再次单击全屏图片会直接缩回画格编组台，不提供多档二次放大。
- 用户发现 `P01-R-G03` 左边界误带入相邻画格；已将 bounds 从 `x=61,width=15` 修正为 `x=63,width=14`。后续分格必须沿黑框或白色分格沟槽内侧裁切，四边复核，不得带入相邻格的人脸、肢体或文字。
- 用户指出 `P01-R-G05` 也切错：旧范围带入左侧相邻大格和右下留白；已从 `x=79,y=67,width=18,height=31` 收紧为 `x=81,y=67,width=14,height=26` 并重新生成裁图。
- 用户指出 `P01-R-G04` 裁掉过多：旧范围右侧脸部与对白被截断；保持 `x=50,y=31,height=35`，将 `width=11` 放宽到 `13`，止于与 `P01-R-G03` 之间的白色分格线。
- 用户指出 `P04-L-G05` 裁切越界且对白开头被截：旧范围 `x=3,y=38,width=23,height=29` 同时带入右侧相邻格和下方对白框；已改为 `x=6,y=31,width=17,height=34`，并使用顶部白色遮罩清理因对白框跨出黑框而残留的上一格细边。当前裁图完整保留“在这里跟我待在一起比较安全”与“你已经被犯人盯上了吗”两组对白，不再出现右侧和下方相邻格。
- 2026-09-02 裁框标准：黄色框是用户手画的标准答案，不是漫画内容或旧模型坐标；实际裁图不得反向覆盖黄色框。裁框使用原始分辨率上的 Box-to-Box 图像分割，先识别完整白色分隔带，再取当前画格一侧黑线；缺线时取白带内缘。两到三条可靠边可补矩形，保留对白文字，真实叠格允许矩形重叠。
- 2026-09-03 待实施的裁图审核链固定为：Agent 初裁候选黄框 → 人工审核并可拖动、缩放、新建、删除、拆分、合并和调整阅读顺序 → 将人工确认框以原图像素坐标锁定 → 按锁定框确定性正式裁图 → AI 只复核切字、相邻格污染、遗漏和留白，不自动改框。人工框是最终标准答案，Agent 或 AI 不得覆盖；单框修改只增量重裁对应画格，不重跑整页。
- 漫画复杂页的分格优先级由用户锁定为“小框优先、外框最后”：检测阶段从最小的明确封闭框开始锁定，小框不得被后续大框吞并；最后补外围大格，大小框冲突时保小框干净，大框边缘允许轻微受影响。检测顺序不改变最终日漫阅读顺序。对白或动作跨框时优先保留小框完整对白，对白外带入区域可遮白。
- 新漫画分析改为两阶段：第一阶段使用独立结构化 box-to-box 几何框检测，只预测矩形、缺失边、检测顺序、阅读顺序和置信度；允许依据相邻框共享边、白色沟槽、角点和行列对齐补全缺失的1–2条边。第二阶段才结合这些候选框整理对白、人物、剧情和 Shot，并逐框复核低置信度结果。框计划单独保存为 `manga-panel-boxes-<requestId>.committed.json`，最终漫画结果也携带 `panelBoxPlan`。
- 正常画格编组台保持紧凑，只显示小图、Shot 和选择控件，不显示对白或解释。点击小图的放大图标后，弹窗采用“左侧大图、右侧理解”布局，右侧显示本格全部对白原文与说话人/置信度、出场人物、人物关系与剧情作用。旧草稿缺失理解数据时从本地分析结果补齐，但不重置 Shot 编组。
- 放大窗口右侧底部提供醒目的“对这张图的批注”输入框，可纠正裁切、对白、人物关系或剧情理解；内容按 panel id 自动保存到当前独立草稿。正常小图编组界面仍不显示解释或批注框。
- 画格编组台的每个 Shot 现在都有“确认并生成完整提示词”按钮；点击本身即记录该 Shot 已确认。Agent 会逐张打开本 Shot 裁图，综合全部画格对白、人物关系、逐格批注、单镜批注、故事背景与最终成片风格，并允许最多 3 次精确联网查询、5 个可靠来源补充作品背景。结果在编组台下方以可编辑、可复制的完整提示词展示，并保留来源链接。
- 2026-08-08 已按“小框优先、缺边补矩形、外框最后”重新裁切当前草稿 Shot 04–17：前三个已批准 Shot 的 35 张画格与批准时间保持不变；Shot 04 起共有 101 张新裁图，全部 136 张有效画格均且仅分配一次。第8张扫描图左页底部两个上下嵌套区域已进一步拆成4个独立小框。更新通过 `agentRevision` 自动同步到当前浏览器草稿，旧分析、草稿和裁图保存在 `work/shotdirector-recut-backups/` 及对应的 `panel-crops.pre-recut-*` 目录中。
- 单镜画格组合、时长、逐格批注或单镜批注改变时，只撤销该 Shot 的旧确认并标记该镜提示词过期；不得牵连其他 Shot。某个 Shot 的完整提示词成功生成后，该镜立即独立解锁人物／场景／道具上传与生成、分镜图和视频数据，不等待全片其他 Shot。
- 跨镜全局连续层只包括故事背景、最终美术风格和人物画像。每镜的人物图、场景图、道具图、生图提示词、全能参考、分镜图和视频数据使用包含 Shot ID 的独立存储键；同名资产不自动跨 Shot 覆盖或复用。
- 单镜完整提示词资料包固定分为故事背景、时代烙印、人物画像、原作剧情依据、最终美术风格五层，再写本 Shot 的逐秒执行。当前《城市猎人》第3话草稿在旧本地状态缺少背景或风格时，会自动从20镜漫画全片、第二话连续性和可靠联网资料补全；点击生成时也会即时补全，不再用空字段拦截。
- 本话人物关系锁定为：老板＝宝马男＝BMW恶魔；裕子是被宝马男杀害的少女；菜摘是裕子的姐姐；冴羽獠＝城市猎人。
- 用户确认的人物画像硬锁：冴羽獠为全名，30岁、186cm、肩宽且身体强壮；槇村秀幸32岁、180cm、体型偏瘦；亜月菜摘24岁、167cm。该数据优先于旧草稿和联网资料，旧的“菜摘22岁”作废。
- 用户确认时代硬锁为昭和62年（1987年）；当前第三话的故事背景、时代烙印、美术风格、车辆、服装、街景与室内设施统一使用这一年份，不得回退到昭和60年（1985年）。
- 用户确认全片语言与声音硬锁：角色全程只说自然日语；画面、字幕、旁白与声音中不出现中文；全程无BGM，只保留日语对白、同期环境声和动作音效。
- 中文可保留为导演阅读的制作备注：每句日语台词可附中文释义，但必须明确“不朗读、不上字幕、不进画面”。人物表演需有活人感，说话、动作、眼神和情绪形成因果链；每镜一个主事件，主要人物先行动，其他人依次反应，动作后保留情绪收尾，避免木偶站桩和随机小动作。
- Shot 02 的完整提示词已于 2026-08-08 成功生成并保存为 `work/shotdirector-responses/complete-shot-prompt-6638f33e-492f-4e24-9c5f-c15aac293a34.committed.json`，不是生成失败或数据丢失。桥接服务现支持按 Shot 从历史完成文件恢复，前端在当前 Shot 本地状态缺少提示词时会自动取回、写入当前草稿并显示“已从本地恢复”；已实测恢复接口返回 `completed`，提示词长度 2975 字。

## 当前优先风险

- 2026-09-05 模型身份显示分为历史事实与当前选择：旧稿继续显示原 `gpt-5.6-sol · codex`，不回写；使用 JK API 新生成的稿件显示 `JK GPT-5.6 Sol · API`。顶部 Chat / Work 模型与推理深度控件已统一为漫镜白、黑、蓝视觉，不再沿用翠易深绿色皮肤。
- 2026-09-05 修复换模型后无法重新生成：完整提示词 `sourceRevision` 纳入 Chat / Work 模型 ID。生成区显示目标模型、开始时间、上一版标记和完成时间；服务器活动任务会恢复状态，重复点击不再把“已在生成”误报为失败。
- 2026-09-05 严格审核解除 stale/error 死循环：只要当前 Shot 保留非空提示词、原作画格和可用 Reviewer，且同 Shot 没有活动任务，就能提交新的只读审核；提交时对旧稿重新绑定当前证据快照，不要求先反复重生成。

- 2026-09-03 新流程替代上文旧批注入口：全部批注输入和提交入口移除，历史数据仍保留；每个 Shot 改为一个持久化主力 Chat。审核建议只能复制到 Chat，由 Creator 修改提示词；Reviewer 保持独立只审不改，改后重审，不自动批准。
- 生成提示词、Chat、严格审核共用当前项目 Worker 的五槽 FIFO 队列，同一稳定 Shot 互斥；第六个排队。Chat 回合落盘、幂等恢复，结果校验项目/Shot/版本，旧稿保留，冲突候选不覆盖正文。Chat 不生成白模或修改 Shot 结构。

- 2026-09-03 “每行一人”无法 Enter 的根因是 onChange 即 split/trim/filter 再 join 回显；改用按项目/Shot 隔离的原始编辑草稿，实时只保存有效条目。覆盖人物、物品、对白、参考清单、原文、段落必现和全局列表。
- 2026-09-03 新漫画项目的 TOP VIEW / 白模生成链尚未接通：generic 初始化为空场景，完整提示词 Schema 不含结构化站位。已增加未生成提示和空场景锁定/导出保护；不得把这一保护描述成已实现自动生成。

- 2026-09-03 审核控件误用全局 busy：其他 Shot 生成会锁住已就绪 Shot 的 Reviewer 选择与提交。修复为按当前审核及独占任务通道锁定，仍保留版本、模型可用性和原图校验。保存流程增加20秒总超时并取消等待账户刷新，不再在保存返回时用旧 state 覆盖新结果；海外域名 HTTP/2 防止长请求挤占保存连接。

1. 2026-08-22 已加入稳定 `projectUid` / `shotUid`，重编号会保留内部身份，拆格与重组会按画格集合得到新身份；但显示编号仍存放在 `shot.id`，尚未完整迁移为独立 `displayNumber` 字段。
2. 2026-08-22 已提供可下载的 Project Manifest JSON，包含阶段、Shot 身份、审核、批准和视频包状态；尚未实现带素材的 ZIP 项目包与服务端项目级级联删除。
3. 素材实验室的“清空当前”只清前端状态，不删除服务器上传、任务和结果文件。
4. 源数据回写直接覆盖 TS 文件，尚未采用临时文件加原子替换。
5. 主项目浏览器状态使用固定存储键；素材实验室也使用全局固定键，项目命名空间尚未完整建立。
6. `01A`、`01B` 的导航展示仍有专门逻辑，其他带后缀 Shot 不会获得相同分组行为。

## 2026-09-02 人物全局档案

- 全局设定开始使用结构化 `characterProfiles`，每人独立保存人物传、身份关系、外形定妆、服装、表演边界和露脸限制；旧 `characters` 数组作为补充规则继续兼容。
- 「城市猎人」全局文件的首批档案为冄羽獠、槇村秀幸、槇村香。人物关系与登场状态按当前话漫画证据推进，禁止把后续剧情提前灌入前期 Shot。

## 2026-08-22 可信生产内核

- 主链路确定为：漫画入库 → 拆分编组 → 分镜 → 完整提示词 → 人审／独立 AI Reviewer／联网补证据 → 用户明确确认 → LibTV CLI 生视频。
- 顶部生产阶段条由项目真实状态推导，不允许手工跳阶段；独立 Reviewer 通过也不能解锁付费视频，必须等用户签字。
- 全能参考使用场景 → 角色 → 道具 → 其他的确定性顺序，视频包保存 `referenceBindings` 和 `referenceSignature`，自动提示词明确写出 `@图片一` 等绑定。绑定变化时可按稳定 reference id 重排提示词，删除的参考会显式标记，避免静默串图。
- LibTV `--run` 调用已区分普通失败、已创建远端付费任务、提交状态不明三种结果。后两种统一标记 `manual-check-required`，后台任务公开 `remoteTaskId` 与 `retryPolicy`，禁止自动重复付费。
- 当前 LibTV 已真实接入临时分镜图与角色／场景／道具资产图；视频阶段只生成并确认 JSON 包，尚未调用 LibTV 视频节点。
- 后续接视频节点必须先通过 `libtv model search --type video` 与 `libtv model <model>` 读取实时 schema；`modeType`、参考图数量、duration、resolution、enableSound 等不能跨模型硬编码。`libtv node ... --run` 本身同步等待终态，本地桥接应等待进程退出；只有退出失败且已经看到 task id／网络结果不明确时，才进入人工检查状态。

## 推荐开发顺序

- 2026-09-05：按翠易的 KO 通道接入 `ko-gpt-5.6-luna`（konjac.ai，独立密钥、Responses），JK 不变；GLM 5.3 与 Flash 使用 Chat Completions。手动模型测试显式传模型 ID，防止测试 Luna 时误用适配器默认 Sol；历史稿不改。

- 2026-09-05：全局设定新增手动 LLM 测试全部模型／单模型重测面板，展示请求与上游实际模型、耗时、时间和安全失败原因。仅 LOW 小请求测试，原有 Shot 生成和审核固定 MAX 不变。结果在用户项目隔离目录持久化；一次提交不重复付费，失败／重启不自动重试，测试按模型数计入 Gateway 额度。

1. 将 `shot.id` 正式拆为 `displayNumber`，清理 `01A` / `01B` 特例。
2. 项目删除预览和安全级联删除。
3. 带素材与路径重映射的 ZIP 项目包导入/导出。
4. 把已确认视频包接入 LibTV CLI 视频节点，并复用付费任务安全协议。
5. 全局设定传播和依赖失效图。
6. 拆分主页面与桥接服务，补充行为测试。
