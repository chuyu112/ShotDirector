# ShotDirector 项目记忆

更新时间：2026-08-07

## 已核实状态

- 产品与项目文件夹已于 2026-08-07 从“镜审（FrameLock）”统一改名为“镜导（ShotDirector）”；当前根目录为 `/Users/Admin/Desktop/镜导 ShotDirector`。
- 当前目录是源码迁移包，不包含 `.git` 历史。
- `npm test` 当前构建成功并通过 12 项测试；交接手册所写“28 项”与当前包不一致。
- 构建仍有前端 chunk 大于 500 KB 的警告。
- `npm run lint` 通过，无 error，有 15 个 warning，主要集中在 `app/page.tsx` 的 Hook 依赖、未使用参数和原生 `<img>`。
- `app/manga-panel-mapping.mjs` 中旧项目专用修复已经删除；交接手册把它描述为仍含硬编码，属于过时信息。
- Codex CLI 运行参数已加入 `--skip-git-repo-check`，可在不含 `.git` 的源码迁移包中执行素材分析。
- `media-analysis.schema.json` 已移除 `$ref` 的同级 `type` 关键字，当前 GPT 结构化输出接口已实测接受该 schema。

## 当前漫画拆镜草稿

- 2026-08-07 已把用户上传的 `3-1.png`、`3-2.png`、`3-3.png` 拆为《城市猎人》第3话“犯下大错！之卷”的独立审核草稿。
- 结果包含 4 个剧情页、26 个有效画格和 11 个连续编号 Shot，总时长约 1 分 39 秒；脚本体检显示 100% 覆盖、0 条未覆盖。
- 第2话连续性已写入背景：1987 年冬夜新宿／歌舞伎町、冴羽獠左掌贯通伤、黑色 BMW 尚未落网、菜摘与槇村的状态，以及象牙白围巾不得无依据重现。
- 最终风格锁定为 1980 年代日本都市动作电视动画的传统手绘赛璐璐成片风格，并开启了受限联网背景补充。
- 草稿已进入逐镜审核区，保持“独立保存”，不会覆盖原审批；当前 0/11 Shot 已批准，11 个资产包待处理。

## 当前优先风险

1. Shot 只有可变的 `id`，尚无稳定 `shotUid` 与独立 `displayNumber`。
2. 没有 Project Manifest，也没有服务端项目级级联删除接口。
3. 素材实验室的“清空当前”只清前端状态，不删除服务器上传、任务和结果文件。
4. 源数据回写直接覆盖 TS 文件，尚未采用临时文件加原子替换。
5. 主项目浏览器状态使用固定存储键；素材实验室也使用全局固定键，项目命名空间尚未完整建立。
6. `01A`、`01B` 的导航展示仍有专门逻辑，其他带后缀 Shot 不会获得相同分组行为。

## 推荐开发顺序

1. Project Manifest 与稳定 UID 数据模型。
2. 项目删除预览和安全级联删除。
3. 导入事务与项目包导入/导出。
4. 全局设定传播和依赖失效图。
5. 拆分主页面与桥接服务，补充行为测试。
