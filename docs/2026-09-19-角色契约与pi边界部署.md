# 角色契约、pi 边界与 AGENTS.md 部署

参考 proma-ai/Proma 的工程实践（pi 依赖隔离、orchestrator 角色分层、AGENTS.md 工程约定），本批完成三项架构整改并于 **2026-09-19 20:44（Asia/Shanghai）** 部署生产，地址 [漫镜](https://manjing.kakayiduo.cloud/)，服务器 `43.173.105.8`。

## 代码变更

- 提交：`4be03e9`（`codex/local-codex-gpt6` 分支），已推送 GitHub。
- 新增 `runner/agent-role-contract.mjs`：creator/review/memory 三角色的系统策略、会话隔离、历史继承、工具可见性、压缩、持久 Session 唯一来源（Proma orchestrator 契约式分层的轻量落地）。
- `runner/manjing-pi-harness.mjs` / `manjing-agent-runtime.mjs` / `manjing-harness-store.mjs`：8 处散落的 `agentRole === "review"` 硬编码判断改为读角色契约；两处重复的 role 归一化收敛到契约模块。
- `eslint.config.mjs`：`no-restricted-imports` 强制 `@earendil-works/*` 只允许 import 到 `runner/manjing-pi-harness.mjs`（pi 升级时上层零改动）。
- 新增 `tests/agent-role-contract.test.mjs`（4 用例，并入 `npm run test:harness`），强制隔离角色安全不变量。
- 新增 `AGENTS.md`：pi 边界、角色契约唯一来源、多租户数据边界、原子写、测试纪律等工程约定。
- 验证：`npm test` 307/307、`npm run test:server` 116/116、lint 0 error（净增 4 个新用例）。

## 发布版本

- 发布编号：`20260919T123751Z-4be03e9`；镜像 `manjing-server:release-20260919t123751z-4be03e9`。
- 分层构建自上一线上镜像 `release-20260918t160317z-4e59afe`，锁文件未变。
- 发布包 412 个文件全部 SHA-256 校验通过；发布目录不含任何私有 `.env*` 文件。
- `/opt/manjing/current` 已重指新版本；上一版本 `20260918T160317Z-4e59afe` 保留作回滚，更早版本与镜像已清理。

## 线上验证

- 三容器 `manjing-*` 使用新镜像重建，状态 Up 无重启循环；数据卷保留。
- 回环：`/healthz` 200（WAL、schema 4），Web 13300 200。
- 容器内确认 `runner/agent-role-contract.mjs`、`tests/agent-role-contract.test.mjs`、`AGENTS.md` 存在。
- 公网 `https://manjing.kakayiduo.cloud/` 200 + HTTP/2 正常。

## 备注

- 本次改动不触碰任何业务逻辑、数据表或审核状态；Agent 运行时行为与上一版等价（测试强制）。
- 推送 GitHub 时遇网络故障，稍后重试成功。
