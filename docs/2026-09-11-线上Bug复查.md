# 2026-09-11 漫镜线上 Bug 复查

检查对象：[漫镜生产站](https://manjing.kakayiduo.cloud/)，服务器 `43.173.105.8`，当前发布 `20260910T161252Z-d4fb26e`。本轮只读检查生产服务、当前漫画草稿、已提交报告和原有裁图，并检查代码、运行定向回归；未改写生产数据、发起付费模型调用或重新部署。

结论：当前服务正常；确认两项影响审核准确性的内容一致性问题，另发现一项跨 Shot 恢复缺口。以下结论不代表全站无其他问题。

## 1. P1：已固化的画格文字证据与实际裁图错配（已确认）

当前素材任务为 `ebc3fd41-a6a7-476f-baee-0d6b7e510615`。从生产服务器只读取得原有裁图，与该任务已提交分析中的 `sourceObservation`、`textSummary`、`sourceText` 逐项比对：

| 画格 | 实际裁图 | 固化文字证据 | 影响 |
| --- | --- | --- | --- |
| P02-R-G05 | 男子下半张脸、下颌与对白 | 双腕受缚、握拳仰头发力；对白被记作用力拟声词 | 审核把图片中真实存在的构图和对白判成无依据 |
| P02-R-G06 | 男子坐着用力，双腕仍被束缚 | 双腕锁链猛烈断裂、碎片飞散 | 把尚未断链与断链瞬间混淆 |
| P02-R-G08 | 双腕特写，锁链断裂、碎片飞散 | 锁链断裂 | 该格支持断链事件在 G08，不能用错误的 G06 描述推翻它 |

本轮直接查看的仅为上述三格；未将历史 Chat 对其他画格的说法当作已核实事实。原裁图副本保存在 [G05](../exports/2026-09-11-bug-audit/P02-R-G05.webp)、[G06](../exports/2026-09-11-bug-audit/P02-R-G06.webp)、[G08](../exports/2026-09-11-bug-audit/P02-R-G08.webp)，校验值见同目录 `sha256.json`。

历史严格审核报告 `prompt-review-57077726-3ba0-435d-8575-4c6ddc1e1d10.committed.json` 使用 `structured-panel-evidence`，其前三项分别涉及无证据对白、G05 下半脸构图和 G06/G08 断链阶段。当前 GLM 5.3 Reviewer 也配置为这一证据模式，只读取固化文字；输入证据错配会造成误报，Creator 拒绝这些意见不等于任务卡住。

代码证据：`scripts/shotdirector-bridge.mjs:4331` 起从已提交分析取出画格观察、摘要与台词；`:2966` 和 `:3001` 明确文字证据模式不直接看图。需要先核对并修复文字证据与原图的绑定、增加证据失效检查，再重新审核受影响提示词。不能通过放宽审核或自动改动人工裁框消除问题。

## 2. P1：Shot 结构与动作、分段数据不同步（已确认）

本轮读取的当前草稿保存时间为 `2026-09-11 03:04:13`（Asia/Shanghai）。Shot 01 的当前结构是 **26 秒、10 格、00:00–00:26**，但以下字段仍来自旧结构：

- `action` 仍写“估算需要 8 秒，当前设置为 8 秒”，画格顺序也未同步。
- `segments` 仍为 9 段，时间只覆盖 0–8 秒。
- `action`、分段及连续性仍引用已移出该 Shot 的 `P02-R-G04`。
- `negative` 仍把 G06 当作边界末端；当前实际边界已包含 G07、G08。
- 全局时间线中的早期 Shot 区间也与当前逐镜时长不一致。

`app/page.tsx:3562` 的时长更新调整结构并作废旧审核；`scripts/shotdirector-bridge.mjs:2979` 起仍把整个全局设定与 Shot 对象作为当前数据送入审核。保留旧稿有历史价值，但未隔离的旧动作、分段和边界描述会让同一请求携带互相矛盾的依据。修复时应明确结构的权威字段、标记旧派生内容失效，并在用户可复核的流程中重建；不能静默改写用户已经确认的分组和时长。

## 3. P2：超时后切换 Shot，原 Chat 可能持续显示工作中（代码确认，页面待复现）

触发条件：Shot A 的 Chat 原始 POST 遇到 409／504／断连等不确定结果，需要只读查询原回合；用户随后切到 Shot B。

`app/page.tsx:3724` 只为当前 `review.chat.pending` 安装恢复轮询；`app/shot-chat.tsx:14` 的 effect 在当前回合变化时清理旧定时器。其他 Shot 没有持续恢复 Chat 结果的循环，因此 A 在服务端完成后，其本地 pending 可能仍存在，直到用户回到 A 才恢复。

正常 POST 成功返回时，`finishShotChat` 会按稳定 Shot UID 回填，所以该问题不等于所有跨 Shot 工作都会卡住，也没有证据表明结果丢失。同一 Shot 在创作台与审核台之间切换的恢复已在上一批修复；这里是切换不同 Shot 的剩余情况。建议按项目内所有 pending Chat 的稳定身份恢复，继续保持只读查询、不重复付费提交。

## 当前健康与验证边界

- 三个生产容器运行，当前版本和镜像与最近部署一致；公网健康接口 HTTP 200，TLS 验证通过，数据库 WAL／schema 4 正常。
- 当前项目 Worker `connected=true`、`busy=false`，`promptJobs=[]`、`mediaJobs=[]`；当前 Shot 01 Chat 无 pending。此次未发现正在持续卡住的生成或审核任务。
- 本地与生产容器中的 `app/page.tsx`、`app/shot-chat.tsx`、`scripts/shotdirector-bridge.mjs` SHA-256 分别一致，以上代码检查针对当前部署版本。
- 本轮运行 Chat、审核轮询、工作状态协调的定向回归：13 项通过、0 失败。日志见 `exports/2026-09-11-bug-audit/regression.log`。
- `npx tsc --noEmit` 仍报原有 7 项 page／whitebox／DB／Worker 类型错误，不能声称类型检查通过；这些错误不等于 7 个已复现的线上交互 Bug。日志见同目录 `types.log`。
- 浏览器自动化读取页面状态多次超时，因此本轮未完成页面交互复现；HTTP 与后台读取正常，不能将自动化连接问题当作产品故障。没有重新运行真实付费的生成 → 审核 → 采纳全链路。

优先处理证据绑定与结构一致性，再补跨 Shot Chat 恢复。本轮仅新增检查记录与本地证据副本；未实施上述修复。
