# 2026-09-10 审核状态提示

状态：用户确认“部署服务器”后，已于 2026-09-10 05:17:51（Asia/Shanghai）部署到 `manjing.kakayiduo.cloud`（`43.173.105.8`），版本 `20260909T211608Z-d4fb26e`。验证与备份见 [审核修复部署记录](2026-09-10-审核修复部署记录.md)。

## 显示行为

- 审核内容上方显示蓝色状态框、大字“严格审核中”和加载动画，展示当前 Shot、任务绑定的审核模型与开始时间。
- 排队显示“严格审核排队中”及提交时间；断线显示“正在恢复审核任务状态”，沿用原任务查询。
- 当前版本的完整报告恢复后，显示绿色勾选和“严格审核完成”，注明完成时间与问题数量；原有人工确认边界保留。旧版本报告不能点亮当前审核的完成标识。
- 审核按钮保持醒目的蓝色和加载图标，禁止重复提交；对应 Shot 导航同步标识。切回创作台后，审核入口与底部状态继续提示当前审核进度。
- 若当前页面仍保留旧报告，明确显示“上一版审核报告 · 本次审核尚未完成”。
- 使用既有审核任务与控件禁用规则。后续修复了页面轮询丢弃在途报告查询的问题，详见 `2026-09-10-审核恢复卡住修复.md`；没有改动模型调用、证据、报告内容、只读边界、人工批准或任务并发机制。

## 验证

- 实际 React 页面配合隔离样例，验证运行中、排队、断线恢复、返回创作台、切换 Shot 和旧报告标识；无真实模型调用。
- 1440、900、390px 宽度检查通过，审核标题分别使用桌面 22px / 手机 20px；页面无横向溢出。桌面工作台切换前后，切换控件的坐标与尺寸一致。
- 样例审核绑定 Kimi K3，Creator 选择 JK GPT-5.6 Sol，生成任务绑定 GLM-5.3-Flash；各任务状态分别显示，其他 Shot 仍可选择审核模型和提交。
- `npm run build:server` 与最终 `npm test` 通过，生产浏览器包校验使用 `/api`；271 项测试全部通过，`npm run lint -- --quiet` 通过。
- UI 验收服务的模型测试尝试、生成尝试、外部请求均为 0。确认发布前，生产服务器仅做只读诊断；本次发布不重新发起模型任务，不推送 GitHub。

## 代码位置

- `app/prompt-review-notice.tsx`：审核中的醒目状态框。
- `app/page.tsx`：任务身份与标签、审核按钮、Shot 导航、创作台与旧报告提示。
- `app/workbench.css`：共享的生成／审核视觉状态及响应式样式。
- `tests/fixtures/workbench-ui.mjs`：`MANJING_UI_REVIEW_STATE=running|queued` 隔离预览。
