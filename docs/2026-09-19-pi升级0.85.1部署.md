# pi Agent Harness 升级 0.84.2 → 0.85.1 部署

**2026-09-19 23:47（Asia/Shanghai）** 完成升级部署，生产地址 [漫镜](https://manjing.kakayiduo.cloud/)，服务器 `43.173.105.8`。

## 升级内容

- pi 三个包（`pi-agent-core` / `pi-ai` / `pi-coding-agent`）0.84.2 → 0.85.1，精确锁版本（不带 caret），与 Proma 商业版同版本。
- **harness 零代码改动**：昨天落地的 pi import 边界（`runner/manjing-pi-harness.mjs` 唯一入口）+ 角色契约隔离经受住升级考验；全量 311/311 测试通过、lint 0 error。
- 新增 `tests/pi-upgrade-acceptance.test.mjs`（4 用例，并入 `npm run test:harness`）：升版本后必跑的端到端验收——创作者带工具执行、持久 Session 跨 Run 恢复、Reviewer 隔离金丝雀（历史内容不得泄漏进审查提示词）、memory 角色契约。
- 提交：`3b77637`（升级）+ `9e38212`（验收测试），已推送 GitHub。

## 发布版本

- 发布编号：`20260919T153755Z-9e38212`；镜像 `manjing-server:release-20260919t153755z-9e38212`。
- **运行依赖变化 → 放弃分层构建**，改为 `node:22-bookworm-slim` 全量构建（npm ci + apt + libtv 1.1.3，均按根 Dockerfile 原样），镜像 2.85GB（比以往小，因 npm ci 缓存层更干净）。
- 发布包 414 个文件全部 SHA-256 校验通过；不含私有 `.env*`。
- `/opt/manjing/current` 已重指新版本；回滚版本为 `20260919T123751Z-4be03e9`。

## 踩坑记录

- 第一版发布包（`20260919T152908Z-3b77637`）漏带 `Dockerfile.deploy.dockerignore`，BuildKit 回退到根 `.dockerignore` 把 `dist` 排除，verify-server-bundle 报 ENOENT。已删除该目录。
- 教训已写进流程：stage 组装后必须 `ls Dockerfile.deploy.dockerignore` 并 `grep -c "  dist/" release-files.sha256`（应为 117）。

## 线上验证

- 三容器 `manjing-*` Up 无重启循环；`/healthz` 200（WAL、schema 4）；Web 13300 200。
- 容器内确认 `pi-coding-agent@0.85.1`、`tests/pi-upgrade-acceptance.test.mjs`、`runner/agent-role-contract.mjs` 就位。
- 公网 `https://manjing.kakayiduo.cloud/` 200 + HTTP/2 正常。
