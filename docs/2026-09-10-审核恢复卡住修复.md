# 2026-09-10 严格审核恢复卡住修复

状态：用户确认“部署服务器”后，已于 2026-09-10 05:17:51（Asia/Shanghai）与审核状态提示一起部署，版本 `20260909T211608Z-d4fb26e`。验证与备份见 [审核修复部署记录](2026-09-10-审核修复部署记录.md)。

## 现场证据

- 用户截图显示 GLM 5.3 最近调用成功，但当前 Shot 仍显示审核中。
- 服务器确认 Shot 01 的报告已于 2026-09-10 04:04:49（Asia/Shanghai）完成并保存，含 6 项问题；现场工作队列为运行 0、排队 0。
- Run `run-8e7b7461-0732-4f14-914f-5259d3999513` 的查询返回 HTTP 200 / completed / 完整报告。
- 报告与草稿均对应 `project-0g7udvr`、`shot-1bh5ez6`、`review-1ioiam0`；草稿仍是 reviewing，尚未回填报告。结果未丢失，无需重新调用模型。

## 根因与修复

页面的审核恢复 effect 在请求发出前便将恢复键标为已处理。每 1.2 秒一次的健康轮询更新依赖，会取消旧 effect 对结果的接收；替代 effect 又因恢复键相同直接返回，导致已完成的报告无法回填。

`PromptReviewRecoveryReader` 让替代 effect 继续接收同一份在途、已解析的响应，只在当前订阅处理完毕后释放读取。终态处理完成才记住恢复键；等待和网络暂时失败继续查询原 Run。每次只读查询有 15 秒超时，未新增模型提交或自动重试付费任务。恢复身份包含项目 UID、工作区、Shot UID、版本和 Run；原有回填版本检查与人工确认规则保留。

## 验证

- 隔离 React 页面使用 2.5 秒报告响应延迟，覆盖多个 1.2 秒健康轮询。修复前后台已完成且队列为空，页面仍无报告；修复后自动回填报告并解除审核按钮禁用。
- 4 项新回归覆盖慢响应被替代订阅接收、排队后继续查询、跨项目／版本隔离、网络失败后的只读重查。
- 最终 `npm test` 的 271 项测试通过，lint 通过，生产浏览器包的 `/api` 校验通过。
- 桌面、中等宽度和手机检查“严格审核中／严格审核完成”、时间与问题数量；旧报告标识、审核模型、只读报告和人工确认均保留。

本次代码、界面标识与上一次待确认的审核状态改动一并发布；保留原有生产草稿、审核报告和服务配置，不重新发起模型任务，不推送 GitHub。
