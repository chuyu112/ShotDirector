# 代码审查四项修复部署

用户明确要求“推送 部署”。修复于 **2026-09-20 06:43:40（Asia/Shanghai）** 发布至 [漫镜](https://manjing.kakayiduo.cloud/)。

## 代码与发布

- 修复提交：[720832f8c4eee8d428fd4d0ec75a2aeceb48c35a](https://github.com/chuyu112/ShotDirector/commit/720832f8c4eee8d428fd4d0ec75a2aeceb48c35a)，已推送 `origin/codex/local-codex-gpt6`，远端引用核验一致。
- 发布前已 fetch：原分支与本地 `40b19d0` 一致，`origin/main` 无本地缺少的提交，开放 PR 为 0。原有未跟踪故事文本、附件与私密配置未提交。
- 服务器：`43.173.105.8`，主机名 `VM-0-4-debian`，Debian 12，Docker Compose 项目 `manjing`。
- 发布目录：`/opt/manjing/releases/20260919T223930Z-720832f`，`/opt/manjing/current` 已原子切换至此。
- Web / Gateway 镜像：`manjing-server:release-20260919t223930z-720832f`。依赖锁文件与上一线上版本一致，使用旧运行环境分层构建，替换全部应用文件并保留依赖目录。
- 正式容器使用的镜像 ID：`sha256:44deaf0cdcc4be9f78149d038d6a62ed85bc88e6dcf8c76bd5323340dc75a500`。
- 发布包：27,830,751 字节，SHA-256 `0c2aad4cdb68c1986081484a9d2708723f8c7a39795a94c958ab01bb43f18b8e`；423 个文件校验通过。镜像构建和两个正式应用容器均逐项核验。
- 私密环境文件在镜像构建后以 `0600` 放入服务器发布目录，未打入归档或镜像；Compose 解析后的 106 个网关配置项与原容器一致。端口保持 Web `13300`、Gateway `18180`、Nginx `8088`，数据卷仍为 `manjing_manjing-data`。

## 修复内容

1. 生产冒烟临时环境文件改为随机路径和 `0600` 权限，成功、失败与信号终止时清理。
2. Provider 的完整空响应可进入 Pi Harness 有限重试，最多尝试 3 次；断流、HTTP 错误、格式错误、拒答及取消不重提，手动诊断只请求 1 次。
3. review / memory 按角色契约禁用持久会话读写，保留 Run 与事件审计；Creator 会话恢复继续工作。
4. Shot 卡片右侧状态允许列内换行，长文字不覆盖 Shot 标题，时长输入仍可编辑。

## 验证

- 本地全量 `npm test`：类型检查、构建、332 项测试通过；Harness 34 项、lint 与差异格式检查通过。
- 正式 `npm run build:server` 通过，8 个浏览器 JavaScript 文件通过同源 `/api` 检查。
- 本地浏览器检查 1280×720、900×720、390×844、320×568、844×390；长状态、批准标记与时长编辑通过。线上界面未另做完整人工业务验收。
- 新镜像在无外部网络、无生产数据挂载的容器中通过 Harness 34 项、Worker/临时密钥文件集成 4 项测试。
- 正式三个容器运行正常，重启计数为 0；公开首页、`/api/healthz` 与未登录认证状态接口正常，HTTPS / HTTP/2 验证通过。首页引用的 7 个 JS/CSS 资源与本地发布包逐字节一致。
- 备份前确认无活动或启动中的 Worker，优雅停止 Gateway 后备份数据。发布后 1,588 个非认证数据库数据文件与快照 SHA-256 全部一致，无丢失或变化；SQLite `quick_check=ok`，WAL/schema 4，仍为 1 个用户和 1 个项目。
- 部署后执行 `scripts/smoke-production.mjs`：使用独立 tmpfs 容器与临时账户，真实 Creator 经 HTTP → Gateway → Worker → Pi Harness → 模型成功拆出 2 镜（37.7 秒）；真实 Creator / Reviewer 隔离金丝雀通过（10.1 秒）。测试容器结束后清理，不挂载生产数据卷。
- 最后核验：冒烟容器为 0，`/tmp/manjing-smoke-env*` 文件为 0，没有残留临时密钥文件。
- 真实模型冒烟确认了基本链路和隔离；未实际诱发上游完整空响应。重试次数、断流不重提等故障场景由本地与容器内测试替身验证，不把它们称为真实上游故障验收。

## 备份与回滚

- 备份目录：`/opt/manjing/backups/20260919T223930Z-720832f`，权限 `0700`。含完整数据卷 `data.tar`、上一运行包、网关配置、Nginx 配置、容器快照、校验清单与部署后验证结果。
- 三份备份归档在发布后重新核验 SHA-256 通过。上一版目录和镜像 `20260919T183257Z-d7b4df1` 保留。
- 代码回滚入口：`bash /opt/manjing/backups/20260919T223930Z-720832f/rollback.sh`。该脚本只切换旧镜像、配置和 current 指针，不恢复数据卷，避免覆盖发布后新增数据；本次未执行回滚。
- 服务器执行记录：`/opt/manjing/incoming/20260919T223930Z-720832f-deploy.sh`；容器测试日志为同目录 `20260919T223930Z-720832f-container-tests.log`。
- 本地验证摘要保存在 `exports/2026-09-20-review-fix-release/`，未推送 GitHub。
- 本次未改正式项目的漫画裁图、分组、提示词、审核报告或批准状态。
