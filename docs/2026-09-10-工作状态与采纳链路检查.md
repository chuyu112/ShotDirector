# 工作状态提示与采纳链路检查

日期：2026-09-10（Asia/Shanghai）。本检查完成时，本批代码已完成本地验证、尚未部署；当时线上为上一批发布 `20260909T211608Z-d4fb26e`。用户于 2026-09-11 批准后已部署，结果见 [工作状态与 Chat 恢复部署记录](2026-09-11-工作状态与Chat恢复部署记录.md)。

## 本批改动

- 工作状态主标题统一为桌面／900px 下 24px、手机 22px，状态文字至少 16px。蓝色状态框、加载标识、操作按钮、Shot 导航和底部状态同步表达当前任务；普通禁用按钮不冒充工作中。
- 主力 Chat 显示“主力 Agent 正在核对并选择采纳建议”，区分排队、讨论、核对审核意见与恢复任务。任务类型依据本次已提交消息判断，本次模型显示任务绑定值，不能被后来切换的模型选择改写。
- 生成提示词、严格审核、主力 Chat、素材分析／恢复／上传、模型测试、保存等工作提示采用同一字号规则。进行中的任务优先于旧的“审核完成”提示；严格审核当前版本完成后保留绿色勾选和醒目完成标题。
- Chat 恢复轮询移到工作台父级，切换创作台／严格审核台后继续读取原回合。提交遇到 409、504 等不确定结果时保留任务，通过只读查询恢复；每次结果查询包含响应读取在内设 15 秒超时，不自动重复调用模型。
- Worker 的 Chat 公开任务状态包含实际绑定模型与真实排队状态。原有项目／Shot／回合／版本回填校验、旧稿保留、改稿后审核过期和人工批准规则继续生效。
- 手机 Shot 时长控件允许换行，避免长工作提示下整页横向溢出。顶部工作台切换位置保持原有布局。

## 生产后台只读核验

在 `43.173.105.8` 的漫镜项目中核对了 Shot 01 的已提交产物、Chat 状态与保存草稿。工作目录为：

`/data/tenants/usr_ce2d0d87505243c689611f60d40965ae/projects/prj_0e635ef414c04ef79103976876cdc396/work`

稳定身份：项目 `project-0g7udvr`、Shot `shot-1bh5ez6`。

| 环节 | 完成时间（北京时间） | 已确认结果 |
| --- | --- | --- |
| 生成完整提示词 | 09-10 05:59:48 | GPT-5.6 Sol，4808 字符，版本 `complete-2cqcsx` |
| 严格审核 | 09-10 06:20:01 | GLM 5.3，5 项问题，版本 `review-k4y98f`；依据已固化的结构化画格证据 |
| 主力 Agent 处理建议 | 09-10 14:01:27 | `action: revise`，5228 字符；公开回复逐项说明不采纳的原因 |
| 草稿落盘 | 核验时已保存 | 当前正文 5228 字符、旧稿 4808 字符、Chat 无 pending、报告 stale、未批准 |

对应产物位于 `shotdirector-responses/`：

- `complete-shot-prompt-4fbc290b-85b9-48d4-a0cc-6ae03fb45540.committed.json`
- `prompt-review-57077726-3ba0-435d-8575-4c6ddc1e1d10.committed.json`
- `shot-chat-9f56fd57-7c73-42db-a733-232288b4a3aa.committed.json` 及同名 `.state.json`
- 保存草稿为 `shotdirector-draft-state/ebc3fd41-a6a7-476f-baee-0d6b7e510615.json`

这次链路已经完成并回填，保留了旧提示词，改稿需要重新审核。核验时运行／排队任务均为 0；这只是当时的后台快照。

## 发现的内容与证据问题

1. **审核固化文字与 Creator 对图片的描述有冲突。** Creator 的公开回复拒绝了第 1–4 项建议，并声称 G05、G06／G08、G01／G02 的实际裁图内容与 Reviewer 使用的文字证据不同。本次没有人工逐图裁定哪方正确，因此不能把“Agent 拒绝建议”本身当作画格事实已经核实。需要在原裁图旁复核、修正有误的证据后再审。
2. **当前 Shot 元数据仍有旧内容。** 原始保存数据的时长为 26 秒、10 个来源画格，结束于 G08；但 action 仍提到 8 秒和旧顺序／G04，segments 仍为 9 段、0–8 秒，negative 仍禁止超出 G06，continuity 仍包含 G04。全局时间线也保留旧的分镜叙述。Chat 的权限仅限当前提示词，不能自动同步这些结构字段；Creator 对第 5 项建议也明确了这一边界。

以上生产内容和证据只读记录，未改写生产提示词、报告、画格、时长、全局设定或审批。状态提示修复不等于这些内容问题已解决。

## 本地验证

- `npm test`：生产构建成功，273 项测试全部通过。Chat 回归覆盖不确定 HTTP 结果恢复原回合、明确失败退出、排队／恢复／讨论／选择采纳标签。
- `npm run lint -- --quiet` 和 `git diff --check` 通过。
- `npx tsc --noEmit` 仍有 7 项原有错误：主页面两处旧类型问题、白模两处、Cloudflare Workers 类型三处；已与上一批部署源文件比对，本批没有新增类型错误。不能将此项记录为通过。
- 实际 React 页面连接隔离的本地虚拟 API，验证 1440／900／390px：工作标题分别为 24／24／22px；窄屏无整页横向溢出；审核完成为绿色勾选且不表示已批准。
- 验证主力 Agent 排队、HTTP 504 后恢复、切换模型后保留任务原模型，以及切到严格审核台仍能取回 Chat 改稿并将旧报告标记为需重审。回填后的测试草稿保留旧稿、无 pending、无误覆盖候选稿、未批准。
- 虚拟 API 共收到 6 次人工触发的测试提交（4 次 Chat、1 次生成、1 次审核），没有自动重复提交。生成测试用于工作状态观感；该测试夹具未实现完整提示词刷新后的结果恢复接口，未将其当作生成刷新恢复的端到端证明。
- 本次未发起真实模型测试、创作或视频调用；未部署、未推送 GitHub。

## 发布边界

本检查结束时等待用户批准，获准后按现有 Docker Compose 流程构建、备份并发布。发布包含新增 `app/work-status-notice.tsx` 及本批前端、桥接变更；不能仅靠 Git HEAD 打包，因为工作区包含先前已部署及本批未提交文件。生产内容问题另行处理。
