# 顶栏改名即时同步与导出脚本部署

用户两条指令「改名和保存必须是同步的 而且要放在一起」「那应该叫：导出脚本」于 **2026-09-22 06:34（Asia/Shanghai）** 发布至 [漫镜](https://manjing.kakayiduo.cloud/)。

## 背景

- 左侧卡片的「修改名称」只改本地标题、不同步服务器；保存时 `persistProjectSnapshot` 走 `/projects/rename` 且 `appendDate: true`，把项目名改成「名字+日期」再去重。用户截图证实下拉框出现「第7话 死亡黑名单 2026-09-22」而左侧没有，两处名字永远对不上。
- 「导出清单」名不副实：`buildProjectManifest` 只导出项目元数据 + 流水线状态 + 每镜状态字段（不含正文），用户猜测的「所有 Shot 内容 + 全局美术风格」才是它应有的内容。

## 代码与发布

- 修复提交：[32ef1d0baeeff72b884050d5f50c2a51d550dc03](https://github.com/chuyu112/ShotDirector/commit/32ef1d0baeeff72b884050d5f50c2a51d550dc03)，已推送 `origin/codex/local-codex-gpt6`。
- 服务器：`43.173.105.8`，Docker Compose 项目 `manjing`。
- 发布目录：`/opt/manjing/releases/20260921T222955Z-32ef1d0`，`/opt/manjing/current` 已切换至此；上一版 `20260919T223930Z-720832f` 保留可回滚。
- 镜像：`manjing-server:release-20260921t222955z-32ef1d0`，镜像 ID `sha256:837bb7f53cf1883f942ca42a37c6ce1020c44615fead27b8b7dbfe9311a68c3f`。锁文件未变，分层构建（FROM `manjing-server:release-20260919t223930z-720832f`）。
- 发布包 27,882,089 字节，SHA-256 `c1fc91c59a338013a8cd3d518c2d14b9f420479080e5984b41812ce5500490b3`；424 个文件服务器解包后 `release-files.sha256` 校验全 OK。

## 改动内容

1. **改名进顶栏、即时同步**：顶栏新增「修改名称」按钮（与 选择项目/新建项目/加载项目/保存项目 同组），对话框确认后通过 `MANJING_RENAME_PROJECT_EVENT` 交由页面处理器 POST `/projects/rename`（**不带 appendDate**），成功后 `loadSession` 立即刷新下拉框；左侧卡片只保留只读标题。服务器模式走 cookie 鉴权的现有路由，本地模式只改本地标题。
2. **保存不再改项目名**：`persistProjectSnapshot` 删除 `serverProjectId` 参数与 rename POST，保存只写内容；服务器 bundle 中 `appendDate` 已为 0。
3. **导出清单 → 导出脚本**：新增 `buildProjectScript`，导出《项目名-脚本.json》：全局美术设定（storyBackground/finalVideoStyle/characterProfiles 等全部字段）+ 每镜脚本七段正文（人物/物品和场景/剧情/动作/连续/美术风格/DIRECTOR VIEW，空段自动省略）+ 结构化镜头字段（台词/机位/构图/连续性/负面词）+ 完整提示词与审批状态。`buildProjectManifest` 保留，仍服务 agent 契约与生产链测试。
4. 清理左侧卡片改名死代码（`beginRenameProject` 等）与 `project-title-form`/`project-title-edit` 样式。

## 验证

- 本地 `npm test` 全绿（typecheck + build + 334 项）；`npm run test:harness` 34 项通过。
- 新增/更新测试：project-save 首个用例改为只调 `/api/draft-state`、断言结果含「城市猎人」且不含日期；新增「rename lives in the top bar…」契约测试；production-core 新增 `buildProjectScript` 全文导出用例；server-auth-ui 断言顶栏「修改名称」按钮与对话框。
- 服务器：健康检查（WAL、schema 4）、Web 200、公网 200 + HTTP/2 全通；容器内确认前端 bundle 含 `manjing-rename-project` 与「导出脚本」，服务端 bundle `appendDate` 为 0。
- 发版后 `node scripts/smoke-production.mjs` 全过：scratch 容器（tmpfs，不碰生产）+ 临时账户 + 真实 Creator 拆镜 2 个（《巷口》，30.2s）+ Reviewer 隔离金丝雀（32.7s），容器自动销毁。
- 收尾：删除更旧版本目录与镜像（`1d4d37c`、`d7b4df1`），incoming 包已删。

## 回滚

`ln -sfn /opt/manjing/releases/20260919T223930Z-720832f /opt/manjing/current` 后在新旧目录按交接手册 8.1 第 7 步重切 compose 即可回到上一版。
