# 2026-09-10 审核状态与恢复修复部署记录

用户确认“部署服务器”后，于 2026-09-10 05:17:51（Asia/Shanghai）完成发布到 [漫镜](https://manjing.kakayiduo.cloud)，服务器为 `43.173.105.8`。

## 发布内容

- 审核中显示蓝色大字、加载动画、Shot、模型和时间；当前版本报告完成后显示绿色勾选、“严格审核完成”和问题数量。Shot 导航、创作台入口、底部状态同步提示，旧报告单独标记。
- 修复健康轮询取消旧订阅后，报告恢复结果被永久丢弃的问题。替代订阅继续接收同一份响应，终态处理后才记住恢复键；断线继续只读查询原任务，不重新调用模型。
- 保留之前发布的顶部工作台切换固定位置、提示词生成状态提示。Reviewer 只审不改、服务端固定 MAX、人工确认和报告版本隔离规则不变。

## 版本与产物

- 发布编号：`20260909T211608Z-d4fb26e`（编号时间为 UTC）。
- 源码 HEAD：`d4fb26e5fd3325c40c9040e934329c5ee115e58e`；本次包含本地未提交的界面和恢复修复，没有提交或推送 GitHub。
- 镜像：`manjing-server:release-20260909t211608z-d4fb26e`。
- Web / Gateway 运行镜像 ID：`sha256:1d901d20945af2866e351f945153c078497f107bead7791a344d84c6962c1688`。
- 发布包 SHA-256：`57d53503d1824a84e944b7c3f3215fb9611f636b9e60eda327d416ff5f9f4c66`；大小 27,693,860 字节；文件校验清单 376 项。
- `/opt/manjing/current` 指向 `/opt/manjing/releases/20260909T211608Z-d4fb26e`。
- 上一版本：`20260909T193642Z-d4fb26e`，镜像 ID `sha256:f5b1db26e30e71033f01939d9110f97e3dd704b861bbf7aaed85aeb09a88d3ef`。
- 生产配置从上一发布目录复制并确认字节一致；锁文件保持一致。密钥、附件和 exports 未进入发布包或镜像。项目原有 `agent.md`、`memory.md` 已随发布包保留。

## 验证证据

- 发布前本地 `npm run build:server`、`npm test`（271 通过、0 失败）、`npm run lint -- --quiet` 通过；浏览器包使用同源 `/api`。
- 隔离 React 页面复现 2.5 秒报告响应跨越 1.2 秒健康轮询的卡住问题，修复后自动回填。1440、900、390px 的运行、完成和旧报告显示通过；未调用真实模型。
- 服务器隔离容器在无外网、无生产数据挂载条件下成功启动 Worker，返回 `ok=true`、`provider=glm`、`model=glm-5.3-flash`。
- 新镜像及 Web、Gateway 内的 376 项文件 SHA-256 清单通过；三个容器运行，重启计数均为 0。
- 公网首页、`/api/healthz`、`/api/auth/me` 请求通过；本机核对首页 8 个静态资源与本地已验证产物逐字节一致。首页和健康接口均为 HTTP 200 / HTTP/2，TLS 校验通过。
- SQLite `quick_check=ok`，WAL / schema 4；用户、项目数发布前后均为 1。原数据卷 `manjing_manjing-data` 保留，现有项目 Worker 已恢复 active；LibTV 1.1.3、FFmpeg 5.1.9 可用。
- Shot 01 的 GLM 5.3 报告仍为 completed，完成时间 `2026-09-09T20:04:49.662Z`，含 6 项问题。发布前后文件 SHA-256 均为 `c8ec5857f2848acf745cd13e9a3eaf828e6f3cc0894cc7ba4d5414c25af0e2de`，原报告完整保留。
- 本次线上核验覆盖服务、产物、备份和报告持久化；界面交互验收在本地隔离页面完成。未把 HTTP 检查描述为生产账号完整业务验收，未手工改写生产草稿或重新提交审核。用户刷新页面加载新版本后，由原任务恢复逻辑回填报告。

## 备份与回滚

- 备份：`/opt/manjing/backups/20260909T211608Z-d4fb26e`。
- Gateway 优雅退出后保存完整原数据卷 `project-data.tar`，大小 159,293,440 字节；快照 SHA-256 校验通过。同时保存 SQLite、环境配置、Compose、Nginx 配置与上一发布路径/镜像信息。
- 回滚镜像标签：`manjing-server:rollback-release-20260909t211608z-d4fb26e`；旧发布和数据卷均保留。代码回滚使用旧镜像及旧 Compose，不能直接覆盖发布后的数据写入。
- 发布脚本、`activation.log`、元数据、健康检查与成功状态位于 `/opt/manjing/incoming/20260909T211608Z-d4fb26e`，`status=complete`。最终部署记录和本机资源校验另存于该目录，不修改已校验的发布文件清单。
