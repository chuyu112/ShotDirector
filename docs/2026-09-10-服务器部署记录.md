# 2026-09-10 漫镜服务器部署记录

2026-09-10 00:49:28（Asia/Shanghai）已完成发布，随后从服务器和本机分别验证公网入口。

后续于 02:01:22 完成用户指定的正式入口 `https://manjing.kakayiduo.cloud` 切换，证书验证结果见 [漫镜子域名切换记录](2026-09-10-漫镜子域名切换.md)。最新于 05:17:51 发布审核状态与卡住恢复修复，版本 `20260909T211608Z-d4fb26e`，见 [审核修复部署记录](2026-09-10-审核修复部署记录.md)。下文保留首次发布时的版本与入口记录。

## GitHub 与发布版本

- 仓库：[chuyu112/ShotDirector](https://github.com/chuyu112/ShotDirector)。
- 已执行 `git fetch origin` 并通过 GitHub API 核对默认分支 `main`；最新提交为 [`d4fb26e5fd3325c40c9040e934329c5ee115e58e`](https://github.com/chuyu112/ShotDirector/commit/d4fb26e5fd3325c40c9040e934329c5ee115e58e)，标题为 `fix: unify shot workflow state`。
- 本地 HEAD 与 origin/main 无领先或落后提交，GitHub 没有待合并 PR。运行代码与 GitHub main 一致；当前工作区已有的说明文档变更随发布包保留，源码参考附件和 exports 不进入运行镜像。
- 发布编号：`20260909T163919Z-d4fb26e`（编号时间为 UTC）。
- 新镜像：`manjing-server:release-20260909t163919z-d4fb26e`。
- 运行镜像 ID：`sha256:addeb86cc9e10686251fdb0a23c0eb3b51f2dba3b6263412cd72c0b0f3c6295a`。
- 发布包 SHA-256：`05188b2ccac21489719c1be9a4b9daefa39c5bf9cf6a0b95e726dc18a6dcab8c`。

## 已核验的实际部署环境

- 公网入口：[https://kakayiduo.cloud](https://kakayiduo.cloud)。DNS 指向 `43.173.105.8`。
- 当前服务器为 Debian x86_64，使用 Docker Compose 项目 `manjing`，包含 Web、Gateway 和 Nginx 三个容器。仓库中的 Alibaba Cloud Linux/systemd 章节属于另一种部署方案，不能作为这台服务器当前状态的依据。
- 当前发布目录：`/opt/manjing/releases/20260909T163919Z-d4fb26e`；`/opt/manjing/current` 已指向该目录。
- Web 与 Gateway 分别映射到宿主机回环端口 `13300` 和 `18180`；宿主机 Nginx 提供 HTTPS 和 HTTP/2。
- 项目数据继续使用原 Docker 卷 `manjing_manjing-data`，容器内路径 `/data`。
- 生产 `.env.server` 沿用上一发布目录的完整配置，并核对字节一致；未把密钥文件放入发布包或镜像。
- 依赖锁文件与旧生产镜像相同，使用既有 Linux 镜像依赖和本次 `npm run build:server` 构建产物生成新镜像。未升级运行时依赖。

## 验证结果

- `npm run build:server` 通过，8 个浏览器 JavaScript 文件通过同源 `/api` 校验。
- `node --test --test-concurrency=4 tests/*.test.mjs`：267 通过，0 失败，0 跳过。
- `npm run lint -- --quiet`：通过，0 错误。
- 发布包路径安全校验通过；新镜像构建时及两个正式应用容器内，361 个发布文件的 SHA-256 清单均通过。
- 新镜像在无外网、无生产数据挂载的隔离容器中完成真实 Worker 启动，返回 `ok=true`、`provider=glm`、`model=glm-5.3-flash`。
- 预检最初因 `docker run --env-file` 保留配置引号失败；改用 Compose 解析后的环境生成临时私有 env 后通过。失败发生在停止线上服务之前，临时 env 已删除。
- 切换后 Web/Gateway/Nginx 均运行；Web 和 Gateway 重启计数均为 0。
- 公网首页、`/api/healthz`、`/api/auth/me` 和首页 8 个静态资源请求通过；从本机再次验证首页和健康接口均为 HTTP 200、HTTP/2，TLS 证书校验通过。
- SQLite 为 WAL、schema 4，`PRAGMA quick_check=ok`。发布前后用户数和项目数均为 1，数据卷保持原卷。
- 线上 LibTV 1.1.3、FFmpeg 5.1.9 可用，已有项目 Worker 恢复为 active。
- 本次未发起真实模型、短信或视频生成请求。浏览器自动化读取超时，因此未把本次 HTTP 验收表述为完整页面交互或业务流程验收。

## 备份与回滚定位

- 备份目录：`/opt/manjing/backups/20260909T163919Z-d4fb26e`。
- Gateway 按既有 930 秒停止窗口优雅退出后，保存约 153 MB 的完整数据卷快照 `project-data.tar`；快照 SHA-256 复核通过。
- 同时保存 SQLite 文件、环境配置、Compose 文件、宿主机与容器 Nginx 配置、旧发布路径及镜像信息。
- 原发布目录：`/opt/manjing/releases/d4fb26e`。
- 原镜像：`manjing-server:us-20260906-workflow-d4fb26e`；另保留回滚标签 `manjing-server:rollback-release-20260909t163919z-d4fb26e`。
- 发布脚本与日志保存在 `/opt/manjing/incoming/20260909T163919Z-d4fb26e`；成功日志为 `deployment-retry1.log`，`status` 为 `complete`。
- 本次保留旧镜像、旧发布目录和原数据卷。代码回滚应使用上述旧 Compose 配置与镜像；数据恢复需另外核对后续写入，不能直接用旧快照覆盖新数据。

项目原有 `agent.md` 和 `memory.md` 已包含在发布包中。本次没有提交或推送 GitHub 变更。
