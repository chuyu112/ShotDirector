# Creator 空返回重试部署

应用户要求把「模型瞬时空返回」从硬失败改为有限重试，并随本批发版。于 **2026-09-20 02:45（Asia/Shanghai）** 部署生产，地址 [漫镜](https://manjing.kakayiduo.cloud/)，服务器 `43.173.105.8`。

## 背景

- 2026-09-20 凌晨发版冒烟时，Creator 链路遇到上游（GLM）瞬时空正文返回，pi harness 归一化直接抛 `runModel 必须返回正文或工具调用`，整个冒烟进程崩溃；重试即过，判定为瞬时上游问题而非代码缺陷。

## 代码变更

- 提交：`d7b4df1`（`codex/local-codex-gpt6` 分支），已推送 GitHub。
- `runner/manjing-pi-harness.mjs`：`streamSimple` 的单次 `runModel` 调用改为 `runModelWithEmptyRetry`——仅当结果为空正文且无工具调用（`empty_run_model_result`）时最多重试 2 次（共 3 次尝试）；其他错误与取消语义不变，仍不重试。
- `tests/pi-agent-runtime.test.mjs`：新增 2 个用例（瞬时空→第 2 次成功且共调用 2 次；持续空→3 次后仍报错）。
- 验证：`npm test` 314/314、`npm run test:harness` 18/18、lint 0 error。

## 发布版本

- 发布编号：`20260919T183257Z-d7b4df1`；镜像 `manjing-server:release-20260919t183257z-d7b4df1`。
- 分层构建自上一线上镜像 `release-20260919t181503z-1d4d37c`；发布包 418 个文件 SHA-256 全部校验通过。
- `/opt/manjing/current` 已重指新版本；上一版 `20260919T181503Z-1d4d37c` 保留作回滚，更旧版本（`9e38212`、`9a7b258`）目录与镜像已清理。

## 线上验证

- 三容器 `manjing-*` 重建 Up；回环 `/healthz` 200（WAL、schema 4）；公网 200 + HTTP/2。
- 容器内确认 `runModelWithEmptyRetry` 已就位。
- 发版后冒烟全绿：Creator 真模型拆镜 2 个（《巷口》）+ Reviewer 隔离金丝雀。

## 备注

- 重试仅发生在 Worker 内部一次任务中，不改变网关计费的请求计数口径；任务最终结果仍失败时错误信息与此前一致。
