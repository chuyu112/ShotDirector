# Seed 2.1 Pro 加入严格审核与 Opus 置灰部署

应用户要求：Seed 2.1 Pro 加入严格审核模型清单；JK Claude Opus 5 在创作与审核列表中置灰（上游中继缺陷，见下）。本批于 **2026-09-20 02:20（Asia/Shanghai）** 部署生产，地址 [漫镜](https://manjing.kakayiduo.cloud/)，服务器 `43.173.105.8`。

## 背景：JK Claude Opus 5 上游中继缺陷（2026-09-20 实测定位）

- 模型本身正常：鉴权、流式、短提示词、长纯文本输入均能通过。
- 但 jiekou 中继对 Opus 存在确定性缺陷：输入达到中等长度且含结构化/JSON 指令时（产品的模型测试提示词必触发），流在首个 token 前被掐断（`incomplete_stream`，多次复现）；同提示词换 Sonnet 正常，短提示词或长纯文本换 Opus 也正常。判断为中继侧对 Opus 的内容/长度组合处理 bug，非本项目代码问题。
- 处理：`MANJING_JIEKOU_CLAUDE_OPUS_ENABLED=0` 置灰（`MANJING_JIEKOU_` 前缀自动透传 Worker），恢复时改 1 即可，配置解析保留。

## 代码变更

- 提交：`1d4d37c`（`codex/local-codex-gpt6` 分支），已推送 GitHub。
- `server/text-model-catalog.mjs`：`seed-2.1-pro` 的 `reviewEnabled: true`（审核模型 5 → 6）；`jk-claude-opus-5` 增加 `enabledVar: MANJING_JIEKOU_CLAUDE_OPUS_ENABLED` 与 `disabledReason`。
- `.env.server.example`：补开关说明（默认 1）。
- 测试：`reviewModelConfigs` 期望列表加 seed；新增 enabledVar 置灰用例；UI fixture 审核列表加 seed。
- 验证：`npm test` 312/312、`npm run test:harness` 16/16、lint 0 error。

## 发布版本

- 发布编号：`20260919T181503Z-1d4d37c`；镜像 `manjing-server:release-20260919t181503z-1d4d37c`。
- 分层构建自上一线上镜像 `release-20260919t172635z-9a7b258`；发布包 417 个文件 SHA-256 全部校验通过。
- 服务器 `/opt/manjing/.env.server` 追加 `MANJING_JIEKOU_CLAUDE_OPUS_ENABLED=0`（备份 `.env.server.bak-20260920-opus-grey`）。
- `/opt/manjing/current` 已重指新版本；上一版 `20260919T172635Z-9a7b258` 保留作回滚。

## 线上验证

- 三容器 `manjing-*` 重建 Up；回环 `/healthz` 200（WAL、schema 4）；公网 200 + HTTP/2。
- 容器内探测全部审核模型：kimi-k3 ✅ 3.6s、seed-2.1-pro ✅ 3.0s、glm-5.3 ✅ 3.7s、jk-gpt-5.6-sol ✅ 2.6s、jk-gemini-3.8-flash ✅ 3.4s；jk-claude-opus-5 正确置灰（零调用成本，显示「上游中继对该模型流式响应不稳定，暂时停用」）。
- 发版后冒烟全绿（Creator 拆镜 2 个 + Reviewer 隔离金丝雀）。

## 备注

- 冒烟首次运行曾遇到 Creator 模型瞬时空返回导致 pi harness 抛 `runModel 必须返回正文或工具调用`（与本次改动无关，重试即过）；后续可考虑把空返回归类为可重试错误。
- Opus 恢复条件：jiekou 中继修复后，将 `.env.server` 开关改回 `1` 并重建容器即可，无需改代码。
