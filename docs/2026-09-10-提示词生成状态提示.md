# 2026-09-10 提示词生成状态提示

用户要求生成提示词时明显告知用户“正在生成提示词”。2026-09-10 03:37:45（Asia/Shanghai）已更新至 [漫镜正式入口](https://manjing.kakayiduo.cloud)。

## 显示行为

- 点击生成后，提示词面板显示蓝色边框、大字“正在生成提示词”和转动图标；等待过程中持续显示，保留上一版提示词供查看。
- 对应 Shot 的生成按钮保持蓝色并显示生成中；顶部当前 Shot 栏和底部状态同步显示，不再仅变灰或误显示“等待盖章”。
- 排队时明确显示“提示词正在排队”；断线时显示恢复任务状态。任务模型从对应项目、Shot 的服务端任务读取，不随顶部模型选择改变。
- 只显示公开状态信息；没有虚构百分比、隐藏推理或额外模型调用。减少动态效果设置会停用转动动画，保留文字提示。

## 验证

- 使用实际 React 页面及隔离样例，检查运行中和排队状态、生成按钮禁用、底部文字、切换到其他 Shot 后显示对应状态。
- 1440、900、390px 三种宽度检查通过；提示标题桌面 22px、手机 20px，无面板横向溢出。
- 本次任务模型为样例 GLM-5.3-Flash，顶部选择为 JK GPT-5.6 Sol，状态面板仍显示任务绑定模型。
- 生产构建、267 项全量测试、后续 28 项 UI 回归及 lint 均通过；隔离验收的模型测试、生成尝试、外部请求均为 0。
- 公网首页 HTTP 200 / HTTP/2，TLS 验证通过。首页 8 个页面资源的 SHA-256 与本地生产构建全部一致。
- Web、Gateway、Nginx 均运行，重启计数 0；原用户、项目各 1 个，SQLite quick_check 为 ok，备份快照校验通过。

## 发布定位

- 当前发布：`/opt/manjing/releases/20260909T193642Z-d4fb26e`。
- 镜像：`manjing-server:release-20260909t193642z-d4fb26e`。
- 镜像 ID：`sha256:f5b1db26e30e71033f01939d9110f97e3dd704b861bbf7aaed85aeb09a88d3ef`。
- 发布包 SHA-256：`c7688263d53e4017d094e3a1ba77d1b786401905270746d2d962a9a9ca0b2d89`；369 项文件清单通过。
- 备份目录：`/opt/manjing/backups/20260909T193642Z-d4fb26e`。
- 发布日志：`/opt/manjing/incoming/20260909T193642Z-d4fb26e/activation.log`，状态 complete。
- 前一发布 `20260909T182551Z-d4fb26e` 与镜像保留。没有提交或推送 GitHub；原有 agent.md、memory.md 与顶栏切换位置修复保留。
