# 2026-09-10 顶栏切换位置修复

用户要求“创作台 / 严格审核台”切换按钮在两边保持同一位置，不因切换页面移动。2026-09-10 02:27:30（Asia/Shanghai）已更新至 [漫镜正式入口](https://manjing.kakayiduo.cloud)。

后续于 03:37:45 发布 [提示词生成状态提示](2026-09-10-提示词生成状态提示.md)，保留本次顶栏位置修复；下文记录本次发布时的版本。

## 修改

- 两边使用 `app/desk-mode-switch.tsx`，共用按钮文字、顺序、选中态与尺寸。
- 审核台采用与创作台相同的顶栏操作区和三列网格；保存状态列预留同样宽度，避免省略控件后重新分配切换按钮的位置或宽度。
- 移除审核台独立右对齐布局；品牌主标题统一为“漫镜”，模式说明保留在副标题和选中按钮中，避免临界宽度下标题换行引起上下偏移。
- 页面保留稳定滚动条空间。更新 `docs/UI设计规范.md` 中的布局约束。

## 验证

- 使用实际 React 页面和 `tests/fixtures/workbench-ui.mjs` 的隔离样例，通过浏览器进行“创作台 → 严格审核台 → 创作台”往返操作。
- 1920、1281、1280、900、390px 五种宽度均检查按钮容器与子按钮的 `getBoundingClientRect()`；同一宽度下坐标和宽高保持一致。1281px 初次发现 4.49px 的纵向偏移，统一品牌标题后复验为 0。
- 浏览器截图对照确认两边按钮同位。隔离数据服务记录模型测试、生成尝试和外部请求均为 0。
- `npm run build:server`、267 项测试及 `npm run lint -- --quiet` 通过。
- 公网首页 HTTP 200、HTTP/2，TLS 校验通过；首页引用的 8 个 CSS、JavaScript、图标资源逐一与本地生产构建计算 SHA-256，全部一致。
- 线上首页、健康和身份接口正常；三个容器运行，重启计数为 0。原数据卷保留，SQLite `quick_check=ok`，用户和项目仍各 1 个。

## 发布定位

- 服务器：`43.173.105.8`，Docker Compose 项目 `manjing`。
- 发布目录：`/opt/manjing/releases/20260909T182551Z-d4fb26e`，当前指针已切换。
- 镜像：`manjing-server:release-20260909t182551z-d4fb26e`。
- 镜像 ID：`sha256:9f85485cfb02a2f0525fdf15ccedbfc9a3c9c0fbac9c5f21e6d827e2d0dbe3d5`。
- 发布包 SHA-256：`3226b7532bada3673fec4ace8125605a5ae9a3b923ccb02d391643db6ad2258d`；367 项文件清单通过。
- 备份：`/opt/manjing/backups/20260909T182551Z-d4fb26e`；完整数据卷快照 SHA-256 复核通过。
- 日志：`/opt/manjing/incoming/20260909T182551Z-d4fb26e/activation.log`；`status=complete`。
- 前一发布 `20260909T172046Z-d4fb26e` 与镜像保留，可用于应用回滚；不得直接用旧数据快照覆盖后续写入。
- 源码以 GitHub `d4fb26e` 为基础，包含本地修改，未提交或推送 GitHub。原有 `agent.md`、`memory.md` 均保留在发布包中。
