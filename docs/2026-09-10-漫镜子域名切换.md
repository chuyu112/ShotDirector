# 2026-09-10 漫镜子域名切换

用户指定正式入口为 `https://manjing.kakayiduo.cloud`，目标服务器为现有 `43.173.105.8`，复用现有漫镜账户、项目与 Docker 数据卷。

后续于 02:27:30 发布顶栏切换位置修复，当前应用版本见 [顶栏切换位置修复](2026-09-10-顶栏切换位置修复.md)；下文保留本次域名切换时的版本和证书记录。

## 已完成正式切换

2026-09-10 02:01:22（Asia/Shanghai）发布脚本状态为 `complete`，随后从服务器和本机分别复核公网入口。

- 正式入口：[https://manjing.kakayiduo.cloud](https://manjing.kakayiduo.cloud)。
- DNSPod 权威 DNS 与公共递归 DNS 均确认 A 记录指向 `43.173.105.8`。
- 新域名单独使用 Let’s Encrypt 证书，SAN 为 `manjing.kakayiduo.cloud`；证书到期时间为 `2026-12-08 17:02:36 UTC`。
- 宿主机 Nginx 已启用新域名的 HTTPS / HTTP/2；HTTP 返回 308 并跳转到新域名 HTTPS。
- 站点默认地址、分享图片元数据和 Gateway Origin 白名单已包含新域名。
- 当前发布目录：`/opt/manjing/releases/20260909T172046Z-d4fb26e`，`/opt/manjing/current` 已指向该目录。
- 当前镜像：`manjing-server:release-20260909t172046z-d4fb26e`。
- 实际运行镜像 ID：`sha256:10e9100a7aecddfe00d26935d670ffda7b322b61670958d3d82beecdd99f32e2`。
- 发布包 SHA-256：`a335462416407cbcb00a865efa73c279212dbdb0c37ac2c5fa5abc5e94ba8932`。

## 上线验证

- 本机公网首页与 `/api/healthz` 返回 HTTP 200、HTTP/2，TLS 验证结果为 0（通过）。
- `/api/auth/me` 返回 HTTP 200 和未登录状态；公开首页的 8 个静态资源全部返回 200。
- 首页分享图片元数据使用 `https://manjing.kakayiduo.cloud/social-preview.png`。
- 登录端点 OPTIONS 请求返回 `Access-Control-Allow-Origin: https://manjing.kakayiduo.cloud`。
- Web、Gateway、容器 Nginx 均运行，重启计数为 0；两个应用容器内的 365 项发布文件 SHA-256 清单通过。
- Gateway 继续使用原数据卷 `manjing_manjing-data`；切换前备份和切换后在线只读检查的 SQLite `quick_check` 均为 `ok`，用户数和项目数均为 1。
- 数据库为 WAL、schema 4；LibTV 1.1.3 与 FFmpeg 5.1.9 可用。
- Certbot 定时器已启用并运行，续期使用 `/var/www/letsencrypt` 的 HTTP 验证目录。
- 已安装仅针对该证书的 Nginx 重载钩子：`/etc/letsencrypt/renewal-hooks/deploy/50-reload-manjing-nginx`，源码为 `deploy/certbot-reload-manjing-nginx.sh`。
- 单独针对新证书执行 `certbot renew --cert-name manjing.kakayiduo.cloud --dry-run --no-random-sleep-on-renew --non-interactive`，模拟续期成功；随后单独执行重载钩子，Nginx 校验与重载成功。服务器 Certbot 2.1 不支持 `--run-deploy-hooks`，因此两步分别验证。记录位于 `renewal-check.log`，状态为 `complete`。

## 备份与后续维护

- 切换前 Gateway 优雅停止，完整数据卷快照与 SQLite、环境配置、Compose、Nginx、旧发布信息保存在 `/opt/manjing/backups/20260909T172046Z-d4fb26e`。
- `project-data.tar.sha256` 在切换后再次复核通过。
- 原发布目录 `/opt/manjing/releases/20260909T163919Z-d4fb26e` 与原镜像保留，可用于应用回滚；恢复旧数据快照前应另行核对上线后的写入。
- 启用日志：`/opt/manjing/incoming/20260909T172046Z-d4fb26e/activation.log`；同目录 `status` 为 `complete`。
- 域名配置与证书维护记录：`/opt/manjing/domain-migration-20260910/`。
- 后续发布应读取 `/opt/manjing/current/.env.server`；根目录旧环境副本不能代表当前新域名 Origin 配置。
- 本次源码以 GitHub `d4fb26e` 为基础，包含本地新域名修改，尚未提交或推送 GitHub。

## 2026-09-10 01:24 服务器准备结果

- `npm run build:server`、267 项全量测试和 `npm run lint -- --quiet` 均通过。
- 宿主机已安装新域名的 HTTP 证书验证入口。服务器本机及从外部使用 Host/`--resolve` 定向请求验证路径，均收到预期文本 `manjing-domain-preflight-ok`。
- 完整 HTTPS 模板在独立 Nginx 测试配置中通过语法校验；此项检查使用现有证书测试配置语法，不代表新域名已经获得证书。
- 候选发布目录：`/opt/manjing/releases/20260909T172046Z-d4fb26e`。
- 候选镜像：`manjing-server:release-20260909t172046z-d4fb26e`；365 个文件的 SHA-256 清单和无外网、无生产数据挂载的隔离 Worker 启动检查通过。
- 候选环境按 Compose 实际解析，已准备新域名的 Origin 白名单。线上容器、账户和项目数据尚未切换。
- 当时发布脚本状态为 `awaiting-dns`；线上仍是 `20260909T163919Z-d4fb26e`。
- 服务端准备日志与启用脚本位于 `/opt/manjing/incoming/20260909T172046Z-d4fb26e/`；入口配置及原 Nginx 配置备份位于 `/opt/manjing/domain-migration-20260910/`。
- DNSPod 控制台需要登录，本机及服务器没有已配置的腾讯云 CLI。已请用户添加上述 A 记录；不会自动重复申请失败的证书。

## 验收边界

新域名解析、匹配证书、HTTPS 首页、同源 `/api`、Origin 校验以及服务器项目数据检查均已通过。本次未发起真实模型、短信或视频生成请求，也未把 HTTP 验收表述为完整浏览器交互或业务流程验收。
