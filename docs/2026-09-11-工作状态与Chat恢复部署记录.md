# 工作状态与 Chat 恢复部署记录

用户于 2026-09-11 明确“部署 服务器”后，本批于 **2026-09-11 00:18:32（Asia/Shanghai）** 发布到 [漫镜](https://manjing.kakayiduo.cloud)，服务器 `43.173.105.8`。部署脚本状态为 `complete`。

## 发布内容

- 工作状态标题统一放大到 24px（手机 22px），状态文字和工作按钮至少 16px；生成、严格审核、主力 Agent 采纳、分析、上传、恢复、保存等使用明确的蓝色提示和加载标识。
- 主力 Agent 区分排队、讨论、审核意见核对、选择采纳与恢复；Chat、Shot 导航、审核台和底部同步显示当前工作，不再被旧的审核完成提示掩盖。
- 切到严格审核台后仍继续恢复原 Chat 回合；409／504 等不确定响应保留原任务，通过只读查询恢复，不自动重复调用模型。本次模型显示绑定的任务模型。
- 保留顶部工作台切换固定位置、旧稿、报告版本隔离、Reviewer 只审不改、服务端固定 MAX 和人工批准规则。

## 版本与产物

- 发布编号：`20260910T161252Z-d4fb26e`（编号时间为 UTC）。
- 源码 HEAD：`d4fb26e5fd3325c40c9040e934329c5ee115e58e`；包含已验证的本地未提交代码，没有提交或推送 GitHub。
- 镜像：`manjing-server:release-20260910t161252z-d4fb26e`。
- Web／Gateway 镜像 ID：`sha256:3178bc3ab23b02336db8cc2f8b49d25f3ecd24c79b421d425584d098fced4502`。
- 发布包：27,587,863 字节，SHA-256 `1ff45fcb5d88df3bf0f4834fabd2001e58d3662e0851386dee5e9e06e52a3ce1`；文件校验清单 304 项。
- `/opt/manjing/current` 指向 `/opt/manjing/releases/20260910T161252Z-d4fb26e`。
- 以上一已验证镜像 `manjing-server:release-20260909t211608z-d4fb26e` 为运行时基础；依赖锁文件 SHA-256 `547dfabc3b61541314ebb2defd7be33d3d9cbed3047c501c2773619d64e13d85` 与线上一致。
- 生产 `.env.server` 从上一版本复制，字节一致；在镜像构建后才写入发布目录。私密配置、源码附件和 exports 未进入发布包。

## 验证

- 本地 `npm run build:server` 通过，确认 8 个浏览器 JavaScript 文件使用同源 `/api`。打包后的运行时代码与本地已验证文件逐字节一致。
- 上一轮完整验证为 273 项测试通过。本次发布前并发复查为 270 通过、2 失败、1 超时取消，失败发生在本地 Worker 启动检查；其中两个检查在串行复跑通过，五 Shot 生产链路再单独复跑通过（2.77 秒）。没有为通过测试放宽断言、延长测试超时或修改运行时代码；保留初次与复跑日志。
- `npm run lint -- --quiet`、发布脚本语法检查、归档路径检查和 `git diff --check` 通过。上一轮独立 TypeScript 检查的 7 项原有错误未在本批修复，也不标为通过。
- 服务器隔离容器在无外网、无生产数据挂载条件下启动 Worker，通过健康检查：`ok=true`、`provider=glm`、`model=glm-5.3-flash`。环境由 `docker compose config --format json` 解析；临时环境文件已删除。
- 切换前确认没有活动 Worker。新镜像及 Web、Gateway 容器中的 304 项 SHA-256 清单通过；三个漫镜容器运行，重启计数均为 0。
- 公网首页、健康与认证接口返回正常；部署脚本检查首页引用的 8 个静态资源均可通过 HTTPS 读取。SQLite `quick_check=ok`，WAL／schema 4，用户数与项目数均保持为 1。
- 本机随后按浏览器压缩传输方式读取公网 8 个静态资源，全部与本地发布包逐字节一致；确认新主力 Agent 状态文案与 24px／22px 字号变量已在线。首页 HTTP 200、HTTP/2、TLS 校验通过，未登录认证边界正常。结果保存在 `public-verification.json`。
- 数据卷仍为 `manjing_manjing-data`。30 份原有已提交产物发布前后的 SHA-256 全部一致；未手工改写生产草稿、审核报告、画格或审批。
- 页面交互与 1440／900／390px 的字号、工作状态和 Chat 恢复验收在上一轮隔离本地页面完成；本次未调用真实创作、审核、模型测试或视频生成，也未把健康检查当作生产账号全链路验收。

## 备份与回滚

- 备份目录：`/opt/manjing/backups/20260910T161252Z-d4fb26e`。
- Gateway 优雅退出后保存完整数据卷快照 `project-data.tar`：160,030,720 字节，SHA-256 `5f3be8c5f6235def97ce5d90401c764db0efd1d5f01917a9a9ee0be6c625df06`；发布后重新校验通过。另存 SQLite、Compose、环境配置和 Nginx 配置。
- 上一版本目录：`/opt/manjing/releases/20260909T211608Z-d4fb26e`。
- 回滚标签：`manjing-server:rollback-release-20260910t161252z-d4fb26e`；旧镜像 ID 为 `sha256:1d901d20945af2866e351f945153c078497f107bead7791a344d84c6962c1688`。代码回滚使用旧镜像及旧 Compose，不能直接覆盖发布后新增的数据。
- 发布脚本、`activation.log`、包元数据与验证文件保存在 `/opt/manjing/incoming/20260910T161252Z-d4fb26e`。本机日志与发布包保存在 `/tmp/manjing-work-release-yQSPDt`；临时 SSH 认证助手完成后清除。

## 独立的生产内容问题

此前发现的 26 秒 Shot 残留旧 8 秒描述、Reviewer 固化文字证据与 Creator 图片描述冲突，属于内容与证据复核事项。本次状态提示与恢复修复没有改写这些内容，详见 [链路检查](2026-09-10-工作状态与采纳链路检查.md)。
