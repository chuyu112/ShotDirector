# 代码扫描、GitHub 推送与服务器部署

用户明确要求“扫一下代码 推送 github 部署服务器”。本批于 **2026-09-11 15:28:39（Asia/Shanghai）** 完成部署，生产地址为 [漫镜](https://manjing.kakayiduo.cloud/)，服务器 `43.173.105.8`。

## 代码与修复

- 代码提交：[be7bbe27a9594d140e4f4681bc5cb7142220798e](https://github.com/chuyu112/ShotDirector/commit/be7bbe27a9594d140e4f4681bc5cb7142220798e)，已推送 `origin/main`，远端引用核验一致。
- 纳入工作状态放大、固定顶栏、审核恢复、原裁图人工核对与结构同步、七项类型修复。新增跨 Shot Chat 恢复：所有保存的 pending 回合独立查询，切换选中镜头不会停止其他回合；旧工作区查询取消，晚到结果不能串写。
- 公开提交包含代码、测试和项目文档；原裁图、导出数据、参考源码附件和私密配置留在本地。Docker 构建另排除 `exports/`。
- 最终 `npm test`：类型检查、生产构建和 **291 项测试通过，0 失败、0 跳过**。lint、提交差异检查通过；`npm run build:server` 再次构建同源 `/api` 版本并通过产物检查。
- 真实 React 本地页面验证：模拟 POST 504 后切到 Shot 02，Shot 01 后台自动回填、清除 pending 并保留旧稿；Shot 02 正文与聊天不变。参见 [扫描记录](2026-09-11-代码扫描与跨Shot恢复.md)。

## 发布版本

- 发布编号：`20260911T072655Z-be7bbe2`；`/opt/manjing/current` 已指向同名发布目录。
- Web／Gateway 镜像：`manjing-server:release-20260911t072655z-be7bbe2`。
- 镜像 ID：`sha256:bb279c12184eff1dbd3067d4657c8e0ac1715a2345ca8cff2f82ced9ffaf554e`。
- 归档大小：29,936,934 字节；SHA-256：`42f35081ca5882577747db5a7c0fb7d50f588a29f4b12509008e76a212d9d566`。
- 文件清单：353 项；镜像构建及实际 Web、Gateway 容器校验全部通过。私密环境配置在镜像构建后从上一版本复制，字节一致。
- 复用上一版本运行时，依赖图核验仅新增固定的 `@cloudflare/workers-types@4.20260702.1`，无运行时依赖变化；新增类型包从 npm 获取并按锁文件 SHA-512 验证后装入镜像。
- 首个候选 `20260911T072258Z-be7bbe2` 在镜像构建预检停止：生产环境下 `npm ls` 默认隐藏开发依赖，误报类型包为空。修正为显式 `--include=dev` 后重新打包、验证并发布；首次失败没有停止或切换线上服务。

## 线上验证

- 三个漫镜容器运行，重启计数均为 0；Web、Gateway 使用上述同一镜像，原数据卷 `manjing_manjing-data` 保留。
- 隔离且无网络、无生产数据挂载的 Worker 启动检查通过；发布前检查 1 个生产 Worker，活动任务为 0。
- 数据库 `quick_check=ok`，WAL／schema 4 正常；1 个用户、1 个项目。30 份原有已提交产物发布前后的 SHA-256 一致。
- 公网首页、健康与认证接口正常，HTTPS／HTTP/2 验证通过。首页 8 个静态资源与本地已验证发布包逐字节一致；工作状态大字及人工核对入口代码已上线。
- 本轮公网页面的浏览器自动化读取超时；线上验收依据 HTTP、资源字节、容器、数据库与 Worker 检查，本地已完成实际浏览器交互。未将这些检查描述为生产账号的真实付费全链路验收。

## 备份与边界

- 完整备份：`/opt/manjing/backups/20260911T072655Z-be7bbe2`，含数据卷快照、SQLite 和配置。
- 数据快照大小：160,030,720 字节；SHA-256：`33703dc5fa07dcc136072747177277f8a462e554f0d7b3503b23160f39edab25`，发布后重新校验通过。
- 可回滚版本：`20260910T161252Z-d4fb26e`；镜像标签 `manjing-server:rollback-release-20260911t072655z-be7bbe2`。回滚代码不应覆盖发布后新增的数据。
- 部署日志与发布包在服务器 `/opt/manjing/incoming/20260911T072655Z-be7bbe2`，本机材料在 `/tmp/manjing-scan-release-dH825j`；测试和核验摘要另存本地 `exports/2026-09-11-scan-release/`。

人工核对入口已上线，**生产 Shot 01 的十格事实和结构仍需用户核对确认**。本次部署没有自动更改漫画事实、分组、时长或批准状态，也没有重新提交生成、审核或视频付费任务；后续改稿仍须重新严格审核并由用户批准。
