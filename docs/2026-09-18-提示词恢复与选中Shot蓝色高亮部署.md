# 提示词恢复与选中 Shot 蓝色高亮部署

他人提交 `b3a5e5e`（Recover orphaned prompt generation states）需要推送与部署；同一批顺带实现用户要求的「选中 Shot 整个变成蓝色」。本批于 **2026-09-18 06:14（Asia/Shanghai）** 完成部署，生产地址为 [漫镜](https://manjing.kakayiduo.cloud/)，服务器 `43.173.105.8`。

## 代码变更

- `b3a5e5e`（chuyu112）：孤儿提示词生成状态恢复。新增 `app/complete-prompt-recovery.mjs` 与 `tests/complete-prompt-recovery.test.mjs`，`app/page.tsx` 接入恢复逻辑；推送前远端核验该提交已在 GitHub，无需本地重复推送。
- `27edcff`（本批）：`app/globals.css` 选中态改为整格蓝色——`.shot-strip button.is-current` 由蓝字 + 4px 底条改为 `background: var(--blue); color: white`（原 `::after` 底条移除，工作中标签改白色 85%）；`.shot-subshot-options button.active` 由细蓝边框 + 浅蓝底改为实心蓝底白字，hover 样式保持原浅蓝不变。
- 两个提交均已推送 `origin/codex/local-codex-gpt6`，本地与远端引用一致。

## 验证

- `npm run typecheck` 0 错误；`npm run test:server` 116/116 通过；`tests/complete-prompt-recovery.test.mjs` + `tests/rendered-html.test.mjs` 23/23 通过；`npm run build:server` 通过（8 个浏览器 JS 同源 `/api` 校验）。
- 锁文件未变：本地与线上容器内 `package-lock.json` SHA-256 均为 `4b00612a929d8b41815e41137e4c19daba8029cd840ef499408d8901d7bdc3e4`，`runtimeDependenciesUnchanged: true`。

## 发布版本

- 发布编号：`20260917T221007Z-27edcff`；镜像 `manjing-server:release-20260917t221007z-27edcff`。
- 分层构建自上一线上镜像 `release-20260917t122319z-badb7da`（镜像 ID `d12786c228d1` 校验一致），仅替换 `/app` 内容。
- 发布包 27,852,260 字节，SHA-256：`f13cd9c1b384e6a8c39cfe53108b28d81a4781a04778615e937fc1fbb521c380`；文件清单 407 项，服务器解包后及 web/gateway 两个容器内 `release-files.sha256` 全量校验均通过。本次打包经 `git archive` + `COPYFILE_DISABLE=1 tar --no-xattrs`，不再含上一批的 macOS `._*` 附加文件，也不含任何 `.env*` 私有文件（`.env.server.example` 除外）。
- 解包位于 `/opt/manjing/releases/20260917T221007Z-27edcff`，`/opt/manjing/current` 与 `/opt/manjing/.release-version` 已更新；`/opt/manjing/.env.server` 的 `MANJING_IMAGE_TAG` 已同步为新标签（原值 `us-20260906-shot-sync-d079ba6` 为历史遗留，与运行实例长期不一致）。

## 部署过程与线上验证

- 复用并适配历史 `deploy-release.sh`（改为从 `/opt/manjing` 的 Compose 工作目录切换，不再处理 `local-codex.env`）；脚本与日志在 `/opt/manjing/incoming/20260917T221007Z-27edcff/`。
- 切换前：隔离无网 Worker 冒烟 `ok:true, provider glm, glm-5.3-flash`；生产 Worker 检查 `workersChecked:1, busyWorkers:0`。
- 备份 `/opt/manjing/backups/20260917T221007Z-27edcff`：含 `.env.server`、Compose 文件、SQLite（`quick_check=ok`，1 用户 1 项目）与完整数据卷快照及 SHA-256；旧镜像已打 `manjing-server:rollback-release-20260917t221007z-27edcff` 回滚标签。
- Gateway 优雅停止（非 137），数据卷 `manjing_manjing-data` 保留并重新挂载校验一致。
- 回环健康 `ok:true`、WAL、schema 4、LibTV 1.1.3、FFmpeg 5.1.9 正常；公网首页 200、`/api/healthz` 正常、首页 8 个静态资源全部 200。
- 本机独立复核：线上 CSS `/_next/static/css/index.DKozDuUx.css` 已含 `.shot-strip button.is-current{background:var(--blue);color:#fff}` 与分镜选项实心蓝规则。

## 备注

- 本次未发起任何真实模型调用、付费生成或 LibTV 任务。
- 服务器 `.env.server` 中历史变量 `MANJING_DAILY_SUPERADMIN_AI_REQUESTS` 仍为无害遗留（上一批已记录），本次未清理。
- 上次部署残留在发布目录的 `._*` AppleDouble 文件不影响运行，本批新发布目录已干净。
