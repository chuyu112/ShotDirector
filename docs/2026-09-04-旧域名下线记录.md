# `manjing.jadecircle.cn` 旧站下线记录

日期：2026-09-04  
执行对象：`manjing.jadecircle.cn` 旧服务器（`121.41.54.168`）

## 执行结果

- 已停止并删除旧站的漫镜 systemd 服务。
- 已停止并删除旧站的证书续期定时任务。
- 已删除旧域名的 Nginx 站点入口。
- 已删除旧程序目录 `/opt/manjing`，删除前约占用 9.7 GB。
- 已确认旧程序使用的端口不再监听。
- 已确认 `https://manjing.jadecircle.cn/` 不再提供漫镜页面。

## 保留内容

- 项目数据库、用户资料及上传素材目录 `/var/lib/manjing` 已保留，未删除。
- 保留数据约 49 MB。
- 旧域名证书文件未作为程序代码删除，但其专用自动续期任务已经停用并移除。

## 新站状态

- 当前正式站点：`https://kakayiduo.cloud/`
- 验证时首页返回 HTTP `200`。
- 当前部署版本：`c4ad15f826516f6c122fe31e14614323a4372aa0`
- 当前服务器镜像：`manjing-server:us-20260904-naming-c4ad15f`

## 磁盘结果

- 旧服务器系统盘：40 GB。
- 删除完成后已用约 9.1 GB，剩余约 29 GB。

## 恢复说明

旧服务器上的 `/opt/manjing` 程序目录已直接删除，不能从该服务器原地恢复。若未来确需恢复旧版本，只能根据 GitHub 提交历史重新构建和部署。保留的 `/var/lib/manjing` 数据可以作为数据迁移或人工核查来源，但不得自动覆盖当前正式站数据。
