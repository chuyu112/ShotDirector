# 选中 Shot 可见性修复与部署

用户反馈真正选中的 Shot 标识太不明显：镜头卡片上的 SHOT 编号按钮点击后没有任何视觉反馈，卡片级蓝色选中框也过淡，且会被"已批准"状态覆盖。本批于 **2026-09-18 21:47（Asia/Shanghai）** 完成部署，生产地址为 [漫镜](https://manjing.kakayiduo.cloud/)，服务器 `43.173.105.8`。

## 代码变更

- 提交：`dee9a09`（`codex/local-codex-gpt6` 分支），已推送 GitHub。
- `app/globals.css`：
  - `.shot-strip button.is-current` 增加墨蓝内描边与底部箭头，`.is-current.is-done` 显式保持蓝色（此前"已完成"的绿色会覆盖选中蓝，导致选中的已批准镜头在满屏绿色中完全不可见）。
  - `.panel-shot-group.is-current` 选中环加深，`.is-current.is-approved/.is-confirmed` 组合显式覆盖（此前卡片顶部 3px 蓝条会被"已批准"绿条顶替）。
  - **当前 Shot 的 SHOT 编号按钮渲染为实心蓝色 pill**——这是用户点选选中目标的直接反馈，此前为无边框透明按钮，点击后无任何变化。
  - `.asset-shot-row.selected` 增加蓝色 outline（此前选中行的蓝色侧条会被 running/stale 等状态色覆盖）。
- `app/workbench.css`：app-shell 主题下 `.panel-shot-group.is-current` 同步强化（蓝色描边 + 外环），并覆盖已批准组合。
- 验证：`tests/rendered-html.test.mjs` + `tests/workbench-style.test.mjs` 24/24 通过；`npm run build:server` 通过。
- 后续按用户反馈调整（同批提交）：不再强调 SHOT 编号按钮（撤销蓝色 pill，恢复为普通文字）；改为**整个标题行（SHOT 编号 + 张图/时长一行）点击即选中**，标题行带手型光标与悬停底色，时长输入框仍阻止冒泡单独可编辑；当前镜头卡片标题行加深蓝色底色，选中指示收敛到卡片级；选中卡片蓝色外环加重为 4px、附加蓝色投影发光，比上一版更明显。线上版本 `20260918T135455Z-3e80154`。

## 发布版本

- 发布编号：`20260918T133942Z-dee9a09`；镜像 `manjing-server:release-20260918t133942z-dee9a09`。
- 分层构建自上一线上镜像 `release-20260917t122319z-badb7da`，锁文件未变。
- 发布包 27,851,545 字节，SHA-256：`63294d415c70a2088cd2400e016f61f18bb8af6044917d5ac0d3809d45956eb2`；密钥预检通过，发布目录不含任何私有 `.env*` 文件。
- `/opt/manjing/current` 已重指新版本；上一版本 `20260917T122319Z-badb7da` 可回滚。

## 线上验证

- 三容器使用新镜像重建，状态 Up 无重启循环；数据卷保留。
- 回环：`/healthz` 正常（WAL、schema 4），Web 200。
- 容器内 `app/globals.css` 已确认包含新选中态规则。
- 公网 `https://manjing.kakayiduo.cloud/` 200 + HTTP/2 正常。

## 备注

- 本次为纯 CSS 变更，不影响任何业务逻辑、数据或审核状态。
- 推送 GitHub 时曾遇网络瞬时故障，已重试成功。
